# 算法导论知识点
> 由 PDF 自动转换为 Markdown（文本提取版）

## 第 1 页

算法导论知识点一览 ( 电情考试 )
记录本书中的伪代码和一些重要证明
0. 时间复杂度详解
O 记号：
o 记号：
课后练习 1

## 第 2 页

- 1.

## 第 3 页

- 2.
- 3.
- 4.

## 第 4 页

常用数学公式和函数
向上取整与向下取整 :
模运算 :
多项式与整数：
- 5.

## 第 5 页

对数：

## 第 6 页

阶乘：

## 第 7 页

多重函数：
斐波那契：

## 第 8 页

## 第 9 页

课后练习 2

## 第 10 页

## 第 11 页

## 第 12 页

## 第 13 页

## 第 14 页

## 第 15 页

第三章思考题

## 第 16 页

## 第 17 页

## 第 18 页

## 第 19 页

## 第 20 页

## 第 21 页

求解递归式

## 第 22 页

0.1 最大子数组问题：
最优股票问题 ..
Find_max_crossing_subarray(A, low, mid, high)
left_sum = -∞
sum = 0
for i = mid downto low
sum = sum + A[i]
if sum > left_sum
left_sum = sum
max_left = i
right_sum = -∞
sum = 0
for j = mid + 1 to high
sum = sum + A[j]
if sum > right_sum
right_sum = sum
max_right = j
return(max_left, max_right, left_sum + right_sum)

## 第 23 页

课后练习 1
Find_max_subarray(A, low, high)
if high == low
return(low, high, A[low])
else mid = floor((low + high)/2)
(left_low, left_high, left_sum) =
Find_max_subarray(A, low, mid)
(right_low, right_high, right_sum) =
Find_max_subarray(A, mid+1, high)
(cross_low, cross_high, cross_sum) =
Find_max_crossing_subarry(A, low, mid, high)
if left_sum >= right_sum and left_sum >= cross_sum
return(left_low, left_high, left_sum)
else if right_sum >= left_sum and right_sum >= cross_sum
return(right_low, right_high, right_sum)
else return(cross_low, cross_high, cross_sum)

## 第 24 页

Iterative_find_max_subarray(A)
n = A.length
max_sum = -∞
sum = -∞
for j = 1 to n
currentHigh = j
if sum > 0
sum = sum + A[j]
else
 currentLow = j

## 第 25 页

0.2 矩阵乘法的 Strassen 算法
0.3 用带入法求解递归式
1. 猜测解的形式
2. 用数学归纳法求出解中的常熟，并证明解是正确的 .
T(n) =2T(floor(n/2)) +n
Guess: T(n) =O(nlogn)
代入法要求证明，恰当选择常数c>0，可有T(n) <=cnlogn。
假定此上界对所有正数 m<n 都成立，特别是 m = floor(n/2) ，有
 sum = A[j]
if sum > max_sum
max_sum = sum
low = currentLow
high = currentHigh
return(low, high, max_sum)

## 第 26 页

课后练习 2

## 第 27 页

4.3 The substitution method for solving recurrences - CLRS Solutions
0.4 递归树求解

## 第 28 页

## 第 29 页

## 第 30 页

## 第 31 页

0.5 主方法

## 第 32 页

Pasted image 20241230205044.png

## 第 33 页

## 第 34 页

第四章思考题：

## 第 35 页

## 第 36 页

## 第 37 页

g.

## 第 38 页

1. 插入排序 (Insertion Sort)
//1. 插入排序
Insertion_sort(A):
for j = 2 to A.length
    key = A[j] //insert A[j] into sorted A[1...j - 1]
    i = j - 1
    while i > 0 and A[i] > key
    A[i + 1] = A[i]
    i = i - 1
A[i + 1] = key

## 第 39 页

如下图为 A = <5, 2, 4, 6, 1, 3> ，演示算法如何工作。
插入排序的时间复杂度为O(n²)，该算法满足循环不定式，并且可以证明。
2. 归并排序 (Merge Sort)
//2. 归并排序
Merge(A, p, q, r)
n1 = q - p + 1
n2 = r - q
let L[1..n1 + 1] and R[1.. n2 + 1] be new arrays
for i = 1 to n1
L[i] = A[p + i - 1]
for j = 1 to n2
R[j] = A[q + j]
L[n1 + 1] = ∞, R[n2 + 1] = ∞
i = 1, j = 1
for k = p to r
    if L[i] <= R[j]
    A[k] = L[i ++]
else A[k] = R[j ++]
Merge_sort(A, p, r)
if p < r
q = (p + r) / 2 //O(1)
Merge_sort(A, p, q) //T(n/2)
Merge_sort(A, q + 1, r) //T(n/2)
Merge(A, p, q, r) / O(n)

## 第 40 页

值得一提的是该伪代码是自顶向下的实现代码，也可以改成自底向上的。 Merge 的时间复杂度是
O(n), 而归并排序的时间复杂度为O(nlogn)。既可以通过递归树来证明，也可以用主方法。
算法导论在这里引入了时间复杂度的详细介绍和分析，主方法算时间复杂度，紧接着引入分治
法。
下图为演示例子：

## 第 41 页

## 第 42 页

algorithm - Exactly how many comparisons does merge sort make? - Stack Overflow
第五章概率算法与随机算法伪代码先鸽了 ...
上图为算导中的贴图，总结了常用算法的时间复杂度。
2.3 课后习题
2.3-2重写 Merge ，不使用哨兵。
MERGE(A, p, q, r)
    n1 = q - p + 1
    n2 = r - q
    let L[1..n1] and R[1..n2] be new arrays
    for i = 1 to n1
        L[i] = A[p + i - 1]
    for j = 1 to n2
        R[j] = A[q + j]

## 第 43 页

i = 1
    j = 1
    for k = p to r
        if i > n1
            A[k] = R[j]
            j = j + 1
        else if j > n2
            A[k] = L[i]
            i = i + 1
        else if L[i] ≤ R[j]
            A[k] = L[i]
            i = i + 1
        else
            A[k] = R[j]
            j = j + 1
2.3-5 Referring back to the searching problem (see Exercise 2.1-3), observe that if the
sequence AA is sorted, we can check the midpoint of the sequence against vv and
eliminate half of the sequence from further consideration. The binary search algorithm
repeats this procedure, halving the size of the remaining portion of the sequence each
time. Write pseudocode, either iterative or recursive, for binary search. Argue that the
worst-case running time of binary search is Θ(lg n).
Iterative_binary_search(A, v, low, high)
while low ≤ high
mid = floor((low + high) / 2)
if v == A[mid]
return mid
else if v > A[mid]
low = mid + 1
else high = mid - 1
return NIL
Recursive_binary_search(A, v, low, high)
if low > high
return NIL
mid = floor((low + high) / 2)
if v == A[mid]
return mid
else if v > A[mid]
return Recursive_binary_search(A, v, mid + 1, high)
else return Recursive_binary_search(A, v, low, mid - 1)

## 第 44 页

The time complexity of the algorithm is Θ(nlg n)+n ⋅ Θ(lg n)=Θ(nlg n)+n ⋅ Θ(lgn)=Θ(nlgn).
著名的两数之和 QAQ 。 LeetCode 第一题。
第二章思考题
2.3-6 Observe that the while loop of lines 5–7 of the Insertion sort procedure in Section
2.1 uses a linear search to scan (backward) through the sorted subarray A[i..j−1]A[i..j−1].
Can we use a binary search (see Exercise 2.3-5) instead to improve the overall worst-case
running time of insertion sort to Θ(nlg n)?
each time the while loop of lines 5-7 of INSERTION-SORTINSERTION-SORT scans
backward through the sorted array A[1..j−1]. The loop not only searches for the proper
place for A[j], but it also moves each of the array elements that are bigger than A[j] one
position to the right (line 6). These movements takes Θ(j)time, which occurs when all
the j−1j−1 elements preceding A[j] are larger than A[j]. The running time of using binary
search to search is Θ(lg j), which is still dominated by the running time of moving
element Θ(j).
Therefore, we can't improve the overrall worst-case running time of insertion sort
to Θ(nlg n).
2.3-7
First, sort S, which takes Θ(nlg n). Then, for each element si in S, i=1,…,n,
search A[i+1..n]for si′=x−si by binary search, which takes Θ(lg n).
If si′ is found, return its position;
otherwise, continue for next iteration.

## 第 45 页

## 第 46 页

COUNT-INVERSIONS(A, p, r)
    if p < r
        q = floor((p + r) / 2)
        left = COUNT-INVERSIONS(A, p, q)
        right = COUNT-INVERSIONS(A, q + 1, r)
        inversions = MERGE-INVERSIONS(A, p, q, r) + left + right
        return inversions
MERGE-INVERSIONS(A, p, q, r)

## 第 47 页

3. 堆排序 (Heap Sort)
    n1 = q - p + 1
    n2 = r - q
    let L[1..n1 + 1] and R[1..n2 + 1] be new arrays
    for i = 1 to n1
        L[i] = A[p + i - 1]
    for j = 1 to n2
        R[j] = A[q + j]
    L[n1 + 1] = ∞
    R[n2 + 1] = ∞
    i = 1
    j = 1
    inversions = 0
    for k = p to r
        if L[i] <= R[j]
            A[k] = L[i]
            i = i + 1
        else
            inversions = inversions + n1 - i + 1
            A[k] = R[j]
            j = j + 1
    return inversions
3. 堆排序
Parent(i):
    return i / 2
Left(i):
return 2i
Right(i):
return 2i + 1
Max_heapify(A, i):
l = Left(i)
r = right(i)
if l <= A.heap_size and A[l] > A[i]
largest = l
else largest = i
if r <= A.heap_size and A[r] > A[largest]
largest = r
if largest != i
exchange A[i] with A[largest]
Max_heapify(A, largest)
// 找出父结点，左孩子，右孩子中，最大的结点，然后与 i 结点交换，接着对递归 largest 。

## 第 48 页

Max_heapify: 其时间复杂度为O(lgn) 树高为 h 来说 O(h)。
Build_max_heap: 可证明为线性时间复杂度。
Heapsort: 时间复杂度为O(nlgn)。
Max_heap_insert, heap_extract_max, Heap_increase_key, Heap_maximum: 时间复杂度为
O(lgn)。
Proofs ：
https://stackoverflow.com/questions/9755721/how-can-building-a-heap-be-on-time-complexity
Build_max_heap(A):
A.heap_size = A.length
for i = A.length/2 downto 1
    Max_heapify(A, i)

Heapsort(A)
Build_max_heap(A)
for i = A.length downto 2
exchange A[l] with A[i]
A.heap_size = A.heap_size - 1
Max_heapify(A, 1)

## 第 49 页

## 第 50 页

typedef struct { int key; int value; } T;
void swap(T a[], int i, int j) {
T t = a[i];
a[i] = a[j];
a[j] = t;
}
int less(T a[], int i, int j) {
return a[i].key < a[j].key;
}
void sift_down(T a[], int i, int j) {
int k;
for (k = (2 * i + 1); k < j; k = (2 * i + 1)) {
if ((k + 1 < j) && less(a, k, k + 1)) { ++k; }
if (less(a, i, k)) {
swap(a, i, k);
i = k;
}
else { break; }
} /* end for */
} /* end sift_down */
void heap_sort(T a[], int n) {
int i, j;
for (i = (n - 1); i >= 0; --i) {
sift_down(a, i, n);
}
for (j = (n - 1); j > 0; --j) {
swap(a, 0, j); sift_down(a, 0, j);
}

## 第 51 页

6.1.1 At least 2h and at most 2h+1−1.
高度从 0 开始计算，叶子为 0 层 ......
6.1.2
6.1.3 反证法
6.1.4
6.1.5 Yes
6.1.6 No
6.1.7
}

## 第 52 页

Max_heapify(A, i)
while true
l = left(i)
r = right(i)
if l <= A.heap_size and A[l] > A[i]
largest = l
else largest = i
if r <= A.heap_size and A[r] > A[largest]
largest = r
if largest == i
return
exchange A[i] with A[largest]
i = largest

## 第 53 页

用归纳法 induction
Heap_increase_key(A, i, key)
if key < A[i]
error"new key is smaller than current key"
while i > 1 and A[PARENT(i)] < key
A[i] = A[PARENT(i)]
i = PARENT(i)
A[i] = key
Heap_delete(A, i)
if A[i] > A[A.heap_size]
A[i] = A[A.heap_size]
Max_heapify(A, i)
else
Heap_increase_key(A, i, A[A.heap_size])
A.heap_size = A.heap_size - 1

## 第 54 页

思考题
def MERGE-SORTED-LISTS(lists)
    n = lists.length
    // Take the lowest element from each of lists together with an index of
the list and make list of such pairs.
    // Pairs are of "type" (element-value, index-of-list)
    let lowest-from-each be an empty array
    for i = 1 to n
        add (lists[i][0], i) to lowest-from-each
        delete lists[i][0]
    // This makes min-heap from list lowest-from-each.
    // We are sssuming that pairs of "type" (element-value, index-of-list)
are compared according to the values of elements.
    A = MIN-HEAP(lowest-from-each)
    let merged-lists be an empty array
    while not A.EMPTY()
        element-value, index-of-list = HEAP-EXTRACT-MIN(A)
        add element-value to merged-lists
        if lists[index-of-list].length > 0
            MIN-HEAP-INSERT(A, (lists[index-of-list][0], index-of-list))
            delete lists[index-of-list][0]
    return merged-lists

## 第 55 页

## 第 56 页

4. 优先队列 (Priority Queues)
优先队列支持以下操作：
Insert(S, x): 把元素 x 插入到集合 S 中。 S = S ∪ {x} 。时间复杂度为O(lgn)。
Maximum(S): 返回 S 中具有最大关键字的元素。时间复杂度为O(1)。
Extract_max(S): 去掉并且返回具有最大键字的元素。O(lgn)。
Increase_key(S, x, k): 将元素 x 的关键字值增加到 k ，这里假设 k 的值不小于原关键字值。O(lgn)。
4. 最大优先队列
Heap_Maximum(A):
return A[1]
Heap_Extract_max(A):
if A.heap_size < 1
error "heap underflow"
max = A[1]
A[1] = A[A.heap_size]
A.heap_size = A.heap_size - 1
Max_heapify(A, 1)
return max

## 第 57 页

5. 快速排序 (Quick Sort)
Heap_increase_key(A, i, key)
if key < A[i]
error "new key is smaller than current key"
A[i] = key
while i > 1 and A[Parent(i)] < A[i]
exchange A[i] with A[Parent(i)]
i = Parent(i)
Max_heap_insert(A, key)
A.heap_size = A.heap_size + 1
A[A.heap_size] = -∞
Heap_increase_key(A, A.heap_size, key)
5. 快速排序 取最后一个数为 q
Quicksort(A, p, r)
if p < r
q = Partition(A, p, r)
Quicksort(A, p, q - 1)
Quicksort(A, q + 1, r)
Partition(A, p, r)
x = A[r]
i = p - 1
for j = p to r - 1
if A[j] <= x
i = i + 1
exchange A[i] with A[j]
exchange A[i + 1] with A[r]
return i + 1

## 第 58 页

快速排序的时间复杂度为O(nlogn)，下图为演示过程。
然而快速排序还有一种随机化优化版本：
上述排序算法，各元素依赖于它们之间的比较，这类排序为比较排序。比较排序可以通过决策树
表示，可以证明他们的最坏时间复杂度为O(nlgn)。
课后练习
5. 快速排序 随机优化
Randomized_partition(A, p, r)
i = Random(p, r)
exchange A[r] with A[i]
return Partition(A, p, r)
Randomized_quicksort(A, p, r)
if p < r
q = Randomize_partition(A, p, r)
Randomized_quicksort(A, p, q - 1)
Randomized_quicksort(A, q + 1, r)

## 第 59 页

## 第 60 页

## 第 61 页

## 第 62 页

## 第 63 页

思考题

## 第 64 页

Partition'(A, p, r)
x = A[p]
low = p
high = p
for j = p + 1 to r
if A[j] < x
y = A[j]
A[j] = A[high + 1]
A[high + 1] = A[low]
A[low] = y
low = low + 1
high = high + 1
else if A[j] == x
exchange A[high + 1] with A[j]
high = high + 1
return (low, high)
Quick_sort(A, p, r)
if p < r
(low, high) = Randomized_partition'(A, p, r)
Quick_sort'(A, p, low - 1)
Quick_sort'(A, high + 1, r)

## 第 65 页

通过比较 q 与 (p + r)/2 ，选择递归调用较小的一部分，从而减少递归深度。
6. 计数排序 (Count Sort)
计数排序假设 n 个输入元素的每一个都是在 0 到 k 区间内的一个整数，其中 k 为某个整数。时间复杂
度为 O(k + n) ，当 k=O(n) 时，排序的运行时间为O(n)。
6. 计数排序
// 对每一个输入元素 x ，确定小于 x 的元素个数。
//B[1..n] 存放排序的输出， C[0..k] 提供临时存储空间。

## 第 66 页

7. 基数排序 (Radix Sort)
基数排序是按最低有效位进行排序来解决卡片排序问题的。如下图所示。
为了保证基数排序的正确性，一位数排序算法必须是稳定的。
Counting_sort(A, B, k)
let C[0..k] be a new array
for i = 0 to k
C[i] = 0
for j = 1 to A.length
C[A[j]] = C[A[j]] + 1 //C[i] contains the number of elements
equal to i
for i = 1 to k
C[i] = C[i] + C[i - 1] //C[i] 现在包括了小于和等于他的元素个数
for j = A.length downto 1
B[C[A[j]]] = A[j]
C[A[j]] = C[A[j]] - 1

## 第 67 页

给定 n 个 d 位数，其中每一个数位有 k 个可能的取值。如果 Radix_sort 使用的稳定排序方法耗时 O(n
+ k) ，那么它就可以在O(d(n + k)) 时间内完成。
8. 桶排序 (Bucket Sort)
桶排序假设输入数据服从均匀分布，平均情况下它的时间代价为O(n)。假设输入是由
一个随机过程产生，该过程将元素均匀，独立地分布在【 0, 1) 区间上。
在代码中，我们假设输入是一个包含 n 个元素的数组 A ，且每个元素 A[i] 满足 0 <= A[i] < 1 。此外算
法还需要一个临时数组 B[0..n - 1] 来存放链表 ( 即桶 ) 。
7. 基数排序
Radix_sort(A, d)
for i = 1 to d
use a stable sort to sort array A on digit i
8. 桶排序
Bucket_sort(A)
n = A.length
let B[0..n - 1] be a new array
for i = 0 to n - 1
make B[i] an empty list
for i = 1 to n
insert A[i] into list B[⌊nA[i]⌋]
for i = 0 to n - 1
sort list B[i] with insertion sort
concatenate the lists B[0],B[1]...B[n - 1] together in order

## 第 68 页

只有插入排序那里时间复杂度为n2 ，但是通过期望分析，总的时间复杂度期望是O(n)，如下图。
9. 栈 (Stacks)
9. 栈的基本操作
Stack_empty(S)
if S.top == 0
return true
else return false

## 第 69 页

三种操作时间均为O(1)。
10. 队列 (Queues)
Push(S, x)
S.top = S.top + 1
S[S.top] = x
Pop(S)
if Stack_empty(S)
error "underflow"
else S.top = S.top - 1
return S[S.top + 1]
Enqueue(Q, x)
Q[Q.tail] = x
if Q.tail == Q.length
Q.tail = 1
else Q.tail = Q.tail + 1
Dequeue(Q)
    x = Q[Q.head]
    if Q.head = Q.length
    Q.head = 1
else Q.head = Q.head + 1
return x

## 第 70 页

两种操作时间均为O(1)。
11. 链表 (Linked Lists)
11. 双向链表
List_search(L, k)// 链表查找 k
x = L.head
while x != null and x.key != k
x = x.next
return x
List_insert(L, x)// 头插法
x.next = L.head
if L.head != null
L.head.prev = x
L.head = x

## 第 71 页

要搜索一个有 n 个对象的链表，过程 List_search 在最坏情况下的运行时间为O(n)，因为可能需要
搜索整个链表。头插法的时间为O(1)。过程 List_delete 将一个元素 x 从链表 L 中移除，该过程要求
给定一个指向 x 的指针，然后通过修改一些指针，将 x 删除出该链表，O(1)。如果要删除具有给定
关键字的元素则要先调用 List_search 找到该元素的指针，O(n)。
x.prev = null
List_delete(L, x)
if x.prev != null
x.prev.next = x.next
else L.head = x.next
if x.next != null
x.next.prev = x.prev
12. 用哨兵版本，哨兵 (sentinel) 的作用是简化边界条件的处理
List_search2(L, k)
x = L.nil.next
while x != l.nil and x.key != k
x = x.next
return x
List_delete2(L, x)
x.prev.next = x.next
x.next.prev = x.prev
List_insert2(L, x)
x.next = L.nil.next
L.nil.next.prev = x
L.nil.next = x
x.prev = l.nil

## 第 72 页

12. 哈希表 (Hash Tables)
许多应用都需要一种动态集合结构，它至少要支持 Insert ， Search ， Delete 字典操作。 hash table
是实现字典操作的一种有效数据结构。尽管在最坏情况下哈希表中查找一个元素的时间与链表中
查找的时间相同，达到了 O(n) 。然而在实际应用中，哈希表的性能是极好的。在一些合理的假设
下，查找时间为 O(1) 。
hash table 是普通数组概念的推广。由于对普通数组可以直接寻址，使得能够在 O(1) 的时间内访
问数组的任意位置。 1. 节讨论直接寻址。如果存储空间允许，我们可以提供一个数组，为每个可
能的关键字保留一个位置，以利用直接寻址技术的优势。
当实际存储的关键字数目比全部的可能关键字总数要小时，采用哈希表就成为直接数组寻址的一
种有效替代，因为哈希表采用一个长度与实际存储的关键字数目成比例的数组来存储。在哈希表
中，不是直接把关键字作为数组的下标，而是根据关键字计算出相应的下标。 2. 节介绍这种技术
的思想，通过链接 (chaining) 方法解决 ” 冲突 “(collision)。冲突就是指多个关键字映射到数组的
同一个下标。 3. 节介绍如何利用哈希函数根据关键字计算出数组的下标。另外，还将介绍和分析
散列技术的几种变形。 4. 节介绍 ”开放寻址法 (open addressing)” ，它是处理冲突的另一种方法。
哈希是一种极其有效和实用的技术，基本的字典操作平均只需要 O(1) 的时间。 11.5 介绍当关键字
集合是静态存储 ( 即关键字存入后就不再改变 ) ， “完全散列”(perfect hashing) 如何能够在 O(1) 的最
坏情况时间内完成关键字的查找。
12.1 直接寻址表
当关键字的全域 U 比较小的时候，直接寻址是一种简单而有效的技术。假设 U 的全域比较小，假
设两个元素没有相同的关键字。我们用一个数组，或称为直接寻址表，记为 T[0..m - 1] 。其中每
个位置称为槽 (slot) 。
12.1 直接寻址表
Direct_address_search(T, k)

## 第 73 页

上述的每一个操作都只需要 O(1) 时间。
12.2 散列表
直接寻址技术的缺点是非常明显的。如果全域 U 很大，那么就需要巨大的存储空间。同时，实际
存储的关键字 K 集合相对 U 来说可能很小。我们能将哈希表的存储需求降低至Θ(|K|)，同时保留
散列表中查找一个元素的优势，只需要 O(1) 时间。不同的是，对于哈希表来说 O(1) 指的是平均时
间，而直接寻址法 O(1) 是指最坏时间复杂度。
在哈希方式下，具有关键字 k 的元素存储在 h(k) 中，利用散列函数 (hash function)h ，由关键字 k 计
算出槽的位置。函数 h 将关键字的全域 U 映射到散列表 T[0..m - 1] 的槽位上。散列函数缩小了数组
下标的范围，即减小了数组的大小，使其由 |U| 减小为 m 。
return T[k]
Direct_address_insert(T, x)
T[x.key] = x
Direct_address_insert(T, x)
T[x.key] = NIL

## 第 74 页

如果两个关键字同时映射到一个槽中，我们称这种情形为冲突 (collision) 。我们需要找到方法来避
免冲突。
12.2 链接法解决冲突
Chained_hash_insert(T, x)
List_insert(T[h(x.key)], x)
Chained_hash_search(T, k)
return List_search(T[h(k)], k)
Chained_hash_delete(T, x)
List_delete(T[h(x.key)], x)

## 第 75 页

证明分析拉链法的性能 暂时省略
在简单均匀散列的假设下，对于用链接法解决冲突的散列表，一次成功查找所需的平均时间为Θ
(1 + α) ，α是 n/m ，能存放 n 的元素的，槽位是 m 。
最后散列表的三个操作平均都可以在 O(1) 内完成。
12.3 散列函数
...
12.4 开放寻址法
在开放寻址法中，所有的元素都放在散列表里。当查找某个元素时，要系统地检查所有的表项，
直到找到所需的元素，或者最终查明该元素不在表中。不像链接法，这里既没有链表，也没有元
素存储在散列表外。因此在开放寻址法中散列表可能会被填满，以至于不能插入任何新的元素。
该方法导致的一个结果便是装载因子α绝对不会超过 1 。
开放寻址法的好处在于它不使用指针，而是计算出要存取的槽序列。不用存储指针而节省的空
间，使得可以用同样的空间来提供更多的槽，潜在地减少了冲突，提高了检索速度。
12.4 开放寻址法解决冲突
Hash_insert(T, k)
i = 0
repeat
j = h(k, i)
if T[j] == NIL
T[j] = k
return j

## 第 76 页

从开放寻址法的散列表中删除操作元素比较困难 ...... 见书本略
更常见的做法是采用链接法来解决冲突。
在我们上面的分析中，做一个均匀散列的假设 ......
有三种技术常用来计算开放寻址法中的探查序列 : 线性探查，二次探查和双重探查。
....
13. 二叉搜索树 (BST)
设 x 是二叉搜索树中的一个结点。如果 y 是 x 左子树中的一个结点，那么 y.key <= x.key 。如果 y 是 x
右子树中的一个结点，那么 y.key >= x.key 。
Theorem12.1 If x is the root of an n-node subtree, then the call Inorder_tree_walk(x) takes Θ(n)
time.
Proof: ...
else i = i + 1
until i == m
error "hash table overflow"
Hash_search(T, k)
i = 0
repeat
j = h(k, j)
if T[j] == k
return j
i = i + 1
until T[j] == NIL or i == m
return NIL
13.1 二叉搜索树的中序遍历 左根右
Inorder_tree_walk(x)
if x != null
Inorder_tree_walk(x.left)
print x.key
Inorder_tree_walk(x.right)
13.2 二叉搜索树查找
Tree_search(x, k)
if x == NIL or k == x.key
 return x
if k < x.key
return Tree_search(x.left, k)
else return Tree_search(x.right, k)

## 第 77 页

Theorem 12.2
The dynamic-set oprations search, minimum, maximum, successor, and predecessor can be
implemented so that each one runs in O(h) time on binary search of height h.
proofs...
迭代的方式重写
Iterative_tree_search(x, k)
while x != NIL and k != x.key
if k < x.key
x = x.left
else x = x.right
return x
最小关键字元素
Tree_minimum(x)
while x.left != NIL
x = x.left
return x
最大关键字元素
Tree_maximum(x)
while x.right != NIL
x = x.right
return x
查找后继
Tree_successor(x)
if x.right != NIL
return Tree_minimum(x.right)
y = x.p
while y != NIL and x == y.right
x = y
y = y.p
return y

## 第 78 页

插入和删除会引起二叉搜索树表示的动态集合的变化。所以我们需要来维护二叉树的性质。
Tree_insert(T, z)
x = T.root
y = NIL
while x ≠ NIL
y = x
if z.key < x.key
x = x.left
else x = right
z.p = y
if y == NIL
T.root = z
else if z.key < y.key

## 第 79 页

与其他搜索树上的原始操作一样。过程 Tree_insert 在一颗高度为 h 的树上的运行时间为O(h)
删除 从一颗二叉搜索树 T 中删除一个结点 z 的整个策略分为三种基本情况，有一种情况有些棘
手。
y.left = z
else y.right = z
如果 z 没有孩子结点，那么只是简单地将它删除，并修改它的父节点，用 NIL 作为孩子结点。
如果 z 只有一个孩子，那么将这个孩子提升到树中 z 的位置上，并修改 z 的父节点，用 z 的孩子
来替换 z 。
如果 z 有两个孩子，那么找 z 的后继 y( 一定在 z 的右子树中 ) ，并让 y 占据树中 z 的位置。 z 的原
来右子树部分成为 y 的新的右子树，并且 z 的左子树成为 y 的新的左子树。这种情况稍显麻

## 第 80 页

为了在二叉搜索树内移动子树，定义一个子过程 Transplant ，它是用另一颗子树替换一颗子树并
成为其双亲的孩子结点。 Transplant 用一颗以 v 为根的子树来替换一颗以 u 为根的子树。
烦，因为还与 y 是否为 z 的右孩子相关。
如果 z 没有左孩子，那么用其右孩子来替换 z ，这个右孩子可以是 NIL ，也可以不是。
如果 z 只有一个左孩子，那么用左孩子来替换 z 。
否则， z 既有左孩子又有右孩子。这个时候我们要查找 z 的后继 y ，这个后继位于 z 的右子树并
且没有左孩子。
如果 y 是 z 的右孩子，那么用 y 替换 z ，并仅留下 y 的右孩子。
否则， y 位于 z 的右子树中但并不是 z 的右孩子。在这种情况下，先用 y 的右孩子替换 y ，
然后再用 y 替换 z 。
Transplant(T, u, v)
if u.p == NIL //u 是树根

## 第 81 页

注意到， Transplant 并没有处理 v.left 和 v.right 的更新，这些更新都由 Transplant 的调用者来负责。
Theorem12.3 The dynamic-set operations INSERT and DELETE can be implemented so that
each one runs in O(h) time on a binary search tree of height h.
14. 红黑树 (Red-Black-Tree)
二叉搜索树可以支持任何一种基本动态集合操作，其时间复杂度均为O(h)。因此如果搜索树的高
度较低时，这些集合操作会执行得比较快。然而，如果树的高度较高时，这些集合操作可能并不
比在链表上执行的快。红黑树是许多平衡树中的一种，可以保证在最坏的情况下基本动态集合操
作时间复杂度为O(lgn)。
红黑树是一课二叉搜索树，它在每个结点上增加了一个存储位来表示结点的颜色，可以是红色或
者黑色。通过对任何一条从根到叶子的简单路径上各个结点的颜色进行约束，红黑树确保没有一
条路径会比其他路径长出 2 倍，因而是近似于平衡的。
一颗红黑树是满足下面红黑性质的二叉搜索树：
T.root = v
else if u == u.p.left //u 是左儿子
u.p.left = v
else u.p.right = v //u 是右儿子
if v != NIL
v.p = u.p // 更新 v.p
Tree_delete(T, z)
if z.left == NIL
Transplant(T, z, z.right)
else if z.right == NIL
Transplant(T, z, z.left)
else y = Tree_minimum(z.right)
if y.p != z
Transplant(T, y, y.right)
y.right = z.right
y.right.p = y
Transplant(T, z, y)
y.left = z.left
y.left.p = y
每个结点或是红色的，或是黑色的
根结点是黑色的
每个叶节点是黑色的
如果一个结点是红色的，则它的两个子结点都是黑色的

## 第 82 页

Lemma 13.1 A red-black tree with n internal nodes has height at most 2lg(n +1).
Proof:
最后，动态集合操作 Search ， Minimum ， Maximum ， Successor 和 Predecessor 可在红黑树上在
O(lgn) 时间内执行。但是 Tree_insert 和 Tree_delete 不能直接用于红黑树的 Insert 和 Delete ，因为
他们并不能保证被这些操作修改过的二叉搜索树任然是红黑树。
对于 Tree_insert 和 Tree_delete 对红黑树做了修改。我们需要维护性质，改变结点的颜色以及指针
结构。指针结构的修改是通过旋转完成的。
对每个结点，从该结点到其所有后代叶结点的简单路径上，均包含相同数目的黑色结点。
为了便于处理红黑树代码中的边界条件，使用一个哨兵来代表 NIL ，哨兵的颜色是黑色。

## 第 83 页

14. 红黑树 左旋
Left_rotate(T, x)
y = x.right
x.right = y.left
if y.left != T.nil
y.left.p = x
y.p = x.p
if x.p == T.nil
T.root = y
else if x == x.p.left
x.p.left = y
else x.p.right = y
y.left = x
x.p = y

## 第 84 页

将结点插入到 T 中，好像 T 是一颗普通的二叉搜索树。插入新结点后将 z 着色为红。为了维护红黑
树性质，我们要调用辅助函数 Rb_insert_fixup 来对结点重新染色并旋转。
14.2 红黑树 插入
Rb_insert(T, z)
y = T.nil //y 将会是 z 的父节点
x = T.root
while x != T.nil
y = x
if z.key < x.key
x = x.left
else x = x.right
z.p = y
if y == T.nil
T.root = z //T 是空树
else if z.key < y.key
y.left = z
else y.right = z
z.left = T.nil
z.right = T.nil
z.color = RED
Rb_insert_fixup(T, z)
Rb_insert_fixup(T, z)
while z.p.color == RED

## 第 85 页

在调用 Rb_insert_fixup 操作时，性质 1 和性质 3 成立。因为新插入的红色结点，所以性质 5 也没
事。这样看来只有可能破坏性质 2 和性质 4 。如果 z 是根结点，则性质 2 不符合。如果 z 的父节点是
红色，则破坏了性质 4 。
if z.p == z.p.p.left //z 的父节点是左孩子吗？
y = z.p.p.right //y is z's uncle
if y.color == RED // are z's parent and uncle bouth
red
z.p.color = BLACK //1
y.color = BLACK //1
z.p.p.color = RED //1
z = z.p.p //1
else
if z == z.p.right
z == z.p //2
Left_rotate(T, z) //2
z.p.color = BLACK //3
z.p.p.color = RED //3
Right_rotate(T, z.p.p) //3
else //same as lines 3-15,but with right and left exchanged
y = z.p.p.left
if y.color == RED
z.p.color = BLACK
y.color = BLACK
z.p.p.color = RED
z = z.p.p
else
if z == z.p.left
z == z.p
Right_rotate(T, z)
z.p.color = BLACK
z.p.p.color = RED
Left_rotate(T, z.p.p)
T.root.color = BLACK

## 第 86 页

上面是一个执行结果。情况 1 会转化为情况 2 ，情况 2 会转化为情况 3 。最后满足所有性质。

## 第 87 页

14.3 红黑树删除
Rb_transplant(T, u, v) // 移动
if u.p == T.nil
T.root = v
else if u == u.p.left
u.p.left = v
else u.p.right = v
v.p = u.p
Rb_delete(T, z)
y = z
y_original_color = y.color
if z.left == T.nil
x = z.right
Rb_transplant(T, z, z.right)
else if z.right = T.nil
x = z.left
Rb_transplant(T, z, z.left)
else y = Tree_minimum(z.right)

## 第 88 页

y_original_color = y.color
x = y.right
if y != z.right
Rb_transplant(T, y, y.right)
y.right = z.right
y.right.p = y
else x.p = y
Rb_transplant(T, z, y)
y.left = z.left
y.left.p = y
y.color = z.color
if y_original_color == BLACK
Rb_delete_fixup(T, x)
Rb_delete_fixup(T, x)
while x != T.root and x.color == BLACK
if x == x.p.left
w = x.p.right
if w.color == RED
w.color = BLACK
x.p.color = RED
Left_rotate(T, x.p)
w = x.p.right
if w.left.color == BLACK and w.right.color == BLACK
w.color = RED
x = x.p
else
if w.right.color == BLACK
w.left.color = BLACK
w.color = RED
Right_rotate(T, w)
w = x.p.right
w.color = x.p.color
x.p.color = BLACK
w.right.color = BLACK
Left_rotate(T, x.p)
x = T.root
else
w = x.p.left
if w.color == RED
w.color = BLACK
x.p.color = RED
Right_rotate(T, x.p)
w = x.p.left

## 第 89 页

if w.right.color == BLACK and w.left.color == BLACK
w.color = RED
x = x.p
else
if w.left.color == BLACK
w.right.color = BLACK
w.color = RED
Left_rotate(T, w)
w.color = x.p.color
x.p.color = BLACK
w.left.color = BLACK
Right_rotate(T, x.p)
x = T.root
x.color = BLACK

## 第 90 页

15. 数据结构的扩张
有许多其他的应用需要对现有的数据结构进行少许地创新和改造，但是只在很少情况下需要创作
出一类全新类型的数据结构。更多的是，通过存储额外信息的方法来扩张一种标准的数据结构，
然后对这种数据结构，编写新的操作来支持所需要的应用。然而对数据结构的扩张并不总是简单
直接的，因为添加的信息必须要能被该数据结构上的常规操作更新和维护。以红黑树为基础。
顺序统计树，简单的在每个结点上存储附加信息的一颗红黑树。添加了 x.size ，这个属性包含了
以 x 为根的子树的结点数，即这棵树的大小。
x.szie=x.left.size+x.right.size+1
在一颗顺序统计树里，我们并不要求关键字各不相同。我们定义一个元素的秩为在中序遍历树时
输出的位置，来消除原顺序统计树定义的不确定性。
介绍一种支持一般动态集合上顺序统计操作的数据结构。快速找到一个集合中第 i 小的数。
或给出一个指定元素在集合的全序中的位置。
抽象出数据结构的扩张过程，给出一个简化红黑树扩张的定理。
使用这个定理来设计一种用于维护由区间构成的动态集合的数据结构。给定一个要查询的区
间，我们能快速的找到集合中一个能与其重叠的区间。
查找具有给定秩的元素 os_select 返回一个指针，指向以 x 为根的子树中第 i 小关键字的结点
Os_select(x, i)
r = x.left.size + 1
if i == r
return x
else if i < r
return Os_select(x.left, i)
else return Os_select(x.right, i - r)

## 第 91 页

每次递归调用就下降一层， Os_select 的运行时间为 O(lgn) 。
...
16. 动态规划 ( 详细包括 CBMS 考题 )
最重要的算法之一。 DP 与分治法类似，都是通过组合子问题的解来求解原问题。分治法将问题
划分为互不相交的子问题，递归地求解子问题，再将它们的解组合起来，求解原问题。动态规划
应用于子问题重叠的情况，即不同的子问题具有公共的子子问题 ( 子问题的求解是递归进行的，将
其划分为更小的子子问题 ) 。在这种情况下，分治算法会做许多重复的工作，反复地求解那些公共
子子问题。而动态规划算法对每个子子问题只求解一次。将其保存在一个表格中，从而无需每次
求解一个子子问题时都重新计算，避免了这种不必要的计算工作。
动态规划方法通常用来求解最优化问题。这类问题可以有很多可行解，每一个解都有一个值，我
们希望寻找具有最优值的解。我们通常用 4 步来设计一个 DP 算法：
1. 钢条切割
钢条切割是指将一条钢材切割，使其达到最大的收益。
rn=max(pn,r1+rn−1,r2+rn−2,...,rn−1+r1)
为了求解规模为 n 的原问题，我们先求解形式完全一样，但规模更小的子问题。即当完成首次切
给定指向 T 中结点 x 的指针，返回对 T 中序遍历对应的线性排序中 x 的位置。
Os_rank(T, x)
r = x.left.size + 1
y = x
while y != T.root
if y == y.p.right
r = r + y.p.left.size + 1
y = y.p
return r
刻画一个最优解的结构特征
递归地定义最优解的值
计算最优解的值，通常采用自底向上的方法
利用计算出的信息构造一个最优解
1 我们会研究一下钢条切割问题， 2 解决用最少的标量乘法操作完成一个矩阵相乘的运算。 3
讨论适合用动态规划方法求解的问题应该具备的两个关键特征。 4 用 DP 找到两个序列的最长
公共子序列。 5 用动态规划方法解决在已知关键字情况下，如何构造最优二叉搜索树。

## 第 92 页

割后，我们将两段钢条看成两个独立的钢条切割问题实例。我们通过组合两个相关子问题的最优
解，并在所有可能的两段切割方案中选取最大者，构成原问题的最优解。钢条切割问题满足最优
子结构性质：问题的最优解由相关子问题的最优解组合而成，而这些子问题可以独立求解。
除了上述公式求解外，还有另外一种相似的求解方法：
rn=max1<=i<=n(pi +rn−i)
自顶向下实现：
Cut_rod 函数的效率是很差的，因为 cutrod 会反复地用相同的参数值对自身进行递归调用，即它反
复求解相同的子问题。下图为 n=4 时的调用过程。
递归调用树中共有2n−1个叶结点，每个叶结点对应一种可能的钢条切割方案。
因此要解决这种反复求解相同子问题的问题， DP 有两种方法。
Cut_rod(p, n)
if n == 0
return 0
q = −∞
for i = 1 to n
q = max(q, p[i] + Cut_rod(p, n - i))
return q

## 第 93 页

子问题图
当思考一个动态规划问题时，我们应该弄清所涉及的子问题及子问题之间的依赖关系。
带备忘录的自顶向下法。仍然按照递归形式编写。但会把每个子问题的解保存下来，当需要
一个子问题的解的时候，检查是否有这个解，如果有就直接返回，没有就计算。
自底向上法，这种方法广受各种刷题达人喜欢。这种方法一般需要恰当定义子问题规模的概
念，使得任何子问题的求解都只依赖于 " 更小的 ” 子问题求解。因而我们可以将子问题按规模
排序，按从小到大的顺序进行求解。当求解某个子问题时，它所依赖的那些更小的子问题都
已求解完毕，结果已经保存。
两种方法具有相同的渐进运行时间，仅有的差异是在某些特殊情况下，自顶向下方法并未真
正递归地考察所有可能的子问题。由于没有频繁的递归函数开销，自底向上的方法时间复杂
性函数通常具有更小的系数。
自顶向下 cutrod
Memo_cut_rod(p, n)
let r[0,..n] be a new array
for i = 0 to n
r[i] = -∞
return Memo_cut_rod_aux(p, n, r)
Memo_cut_rod_aux(p, n, r)
if r[n] >= 0
return r[n]
if n == 0
q = 0
else q = -∞
for i = 1 to n
q = max(q, p[i] + Memo_cut_rod_aux(p, n - i, r))
r[n] = q
return q
// 自底向上版本
Bottom_up_cut_rod(p, n)
let r[0..n] be a new array
r[0] = 0
for j = 1 to n
q = -∞
for i = 1 to j
q = max(q, p[i] + r[j - i])
r[j] = q
return r[n]

## 第 94 页

重构解
我们通过上面给出的算法求出了最优解的收益值，但并未返回解本身。我们可以扩展动态规划算
法，使之对每个子问题不仅保存最优收益值，还保存对应的切割方案。 sj 对应第一段钢条的切割
长度。
2. 矩阵链乘法
对矩阵加括号会对乘积的运算次数产生巨大的影响。选取合适括号的位置是一种减少时间复杂度
的策略。穷举所有可能的括号方案不会产生一个高效的算法。
Extended_bottom_up_cut_rod(p, n)
let r[0..n] and s[0..n] be new arrays
r[0] = 0
for j = 1 to n
q = -∞
for i = 1 to j
if q < p[i] + r[j - i]
q = p[i] + r[j - i]
s[j] = i
r[j] = q
return r and s
Print_cut_rod_solution(p, n)
(r, s) = Extended_bottom_up_cut_rod(p, n)
while n > 0
print s[n]
n = n - s[n]

## 第 95 页

Pn 表示可供选择的括号化方案的数量。可以证明递归公式结果为Ω(2n)。通过暴力搜索穷尽所有
可能的括号化方案来寻找最优方案，是一个糟糕的策略。
利用动态规划方法，按照 4 个步骤来解决问题。
1. 最优括号化方案的结构特征 :
动态规划方法的第一步是寻找最优子结构，然后就可以利用这种子结构从子问题的最优解构
造出原问题的最优解
为了构造一个矩阵链乘法问题实例的最优解，我们可以将问题划分为两个子问题 (
AiAi+1..Ak和Ak+1Ak+2..Aj的最优括号化问题 ) ，求出子问题实例的最优解，然后将子问题
的最优解组合起来。我们必须保证在确定分割点时，已经考察了所有的分割点，这样就可以
保证不会遗漏最优解。
2. 一个递归求解方案:
用子问题的最优解来递归地定义原问题最优解的代价。
3. 计算最优代价
现在，我们可以很容易地基于递归公式写出一个递归算法。但是这种方法并不比检查所有括
号化方案的暴力搜索方法更好。注意到，我们需要求解的不同子问题的数目是相对较少的。
递归算法会在递归调用树的不同分支中多次遇到同一个子问题。这种子问题重叠的性质是应
用动态规划的另一个标识 ( 第一个标识是最优子结构 ) 。
Matrix_chain_order(p)
n = p.length - 1
let m[1..n, 1..n] and s[1..n-1, 2..n] be new tables
for i = 1 to n
m[i, i] = 0
for l = 2 to n
for i = 1 to n - l + 1
j = i + l - 1
m[i, j] = ∞
for k = i to j - 1
q = m[i, k] + m[k + 1, j] + p{i - 1}pkpj

## 第 96 页

Matrix_chain_order 的时间复杂度为O(n3)循环嵌套的深度为三层，每层的循环变量 (l, i 和 k) 最多取
n - 1 个值。
构造最优解代码如下 :
if q < m[i, j]
m[i, j] = q
s[i, j] = k
return m and s
Print_optimal_parens(s, i, j)
if i == j
print "Ai"
else print "("
Print_optimal_parens(s, i, s[i, j])

## 第 97 页

3. 动态规划原理
适合应用动态规划方法求解的最优化问题应该具备的两个要素是最优子结构和子问题重叠。
最优子结构
如果一个问题的最优解包含其子问题的最优解，我们就称其具有最优子结构性质。它是否具有最
优子结构是一个好的线索，当然具有最优子结构性质也可能意味着适合应用贪心策略。使用动态
规划方法时，我们用子问题的最优解来构造原问题的最优解。因此，我们必须小心确保考察了最
优解中用到的所有子问题。
最优
在发掘最优子结构性质的过程中，实际上遵循了，如下的通用模式 :
Print_optimal_parens(s, s[i, j] + 1, j)
print ")"
1. 证明问题最优解的第一个组成部分是做出一个选择，例如，选择钢条的第一次切割位置，选
择矩阵链的划分位置等。做出这次选择会产生一个或者多个子问题。
2. 对于一个给定问题，在其可能的第一步选择中，你假定已经知道哪种选择才会得到最优解。
你现在并不关心这种选择具体是如何得到的，只是假定已经知道了这种选择。
3. 给定可获得最优解的选择后，你确定这次选择会产生那些子问题，以及如何最好地刻画子问
题空间。
4. 利用 "cut-and-paste" 技术证明：作为原问题最优解的组成部分，每个子问题的解就是它本身
的最优解。
一个刻画子问题空间的好经验是，保持子问题空间尽可能简单，只在必要时扩展它。例如，
我们在求解钢条切割问题时，子问题空间中包含的问题为：对每个 i 值，长度为 i 的钢条的最
优切割问题。这个子问题空间很有效，因此我们不必尝试更一般性的子问题空间。
与之相对的，假定我们试图限制矩阵链 A1A2...Aj 乘法问题的子问题空间。如前所述，最优
括号化方案必然在某个位置 k 处，即 Ak 和 Ak+1 之间对矩阵链进行划分。除非我们能保证 k 永
远等于 j-1 ，否则我们会发现得到两个形如 A1A2...Ak+1 和 Ak+1Ak+2...Aj 的子问题，而后者的
形式与 A1A2...Aj 是不同的。因此，对于矩阵链乘法问题，我们必须允许子问题在 " 两端 " 都可
以变化，即允许子问题 AiAi+1...Aj 中 i 和 j 都可变。
对于不同问题领域，最优子结构的不同体现在两个方面：
5. 原问题的最优解中涉及多少个子问题，以及
6. 在确定最优解使用哪些子问题时，我们需要考察多少种选择
在钢条切割问题中，长度为 n 的钢条的最优切割方案仅仅使用一个子问题 ( 长度为 n-i 的钢条的
最优切割 ) ，但我们必须考察 i 的 n 种不同取值，来确定哪一个会产生最优解。 AiAi+1...Aj 的矩
阵链乘法问题中，最优解使用两个子问题，我们需要考察 j-i 种情况。对于给定的矩阵链划分
位置，矩阵 Ak ，我们需要求解两个子问题的括号化方案，而且两个子问题都必须求解最优
方案。一旦我们确定了子问题的最优解，就可以在 j-i 个候选的 k 中选取最优者。
我们可以用子问题的总数和每个子问题需要考察多少种选择这两个因素的乘积来粗略分析动
态规划算法的运行时间。对于钢条切割问题，共有Θ(n)个子问题，每个子问题最多需要考

## 第 98 页

一些细节之处：
在尝试使用动态规划策略要小心，要注意问题是否具有最优子结构性质。
无权最短路径：是具有最优子结构性质
有权最短路径：不具有最优子结构性质
无权最长简单路径问题看起来不像有任何形式的最优子结构，对此问题尚未找到有效的动态规划
算法。实际上，此问题是 NP 完全的，我们不太可能找到多项式时间的求解方法。
为什么最长简单路径问题的子问题结构与最短路径有这么大的差别，原因在于，最短路径子问题
是无关的，这里，子问题无关的含义是，同一个原问题的一个子问题的解不影响另一个子问题的
解。
换一个角度来说，我们所面临的困境是：求解一个子问题时用到了某些资源 ( 上图是顶点 ) ，导致
这些资源在求解其他子问题时不可用。
重叠子问题
适合用动态规划方法求解的最优化问题应该具备的第二个性质是子问题空间必须足够小。即问题
的递归算法会反复地求解相同的子问题，而不是一直生成新的子问题。一般来说不同子问题的总
数是输入规模的多项式函数为好。如果递归算法反复求解相同的子问题，我们就称最优化问题具
有重叠子问题性质。与之相对的，适合用分治法求解的问题通常在递归的每一步都生成全新的子
问题。
察 n 种选择，因此运行时间为O(n2)。矩阵链乘法问题共有Θ(n2)个子问题，每个子问题最多
需要考察 n - 1 种选择，因此运行时间为O(n3)。子问题图也可以用来做相同的分析。图中每
个顶点对应一个子问题，而需要考察的选择对应关联至子问题顶点的边。
矩阵链乘法
Recursive_matrix_chain(p, i, j)
if i = j
return 0
m[i, j] = ∞
for k = i to j - 1
q = Recursize_matrix_chain(p, i, k)

## 第 99 页

我们可以证明此过程计算 m[1, n] 的时间至少是 n 的指数函数。
重构最优解
利用辅助数组重构最优解可以节省很多时间。假设我们没有 s[i, j] 表，只是在表 m[i, j] 种记录了子
问题的最优代价。当我们确定最优括号化方案用到了哪些子问题时，就需要检查所有 j - i 种可能。
而通过在 s 中保存划分位置，我们可以在 O(1) 时间内完成。
备忘
+ Recursize_matrix_chain(p, k + 1, j)
+ pi-1pkpj
if q < m[i, j]
m[i, j] = q
return m[i, j]
自顶向下备忘录版本 线代链
Memoized_m_c(p)
n = p.length - 1
let m[1..n, 1..n] be a new table
for i = 1 to n
for j = i to n
m[i, j] = ∞
return Lookup_chain(m, p, 1, n)
Lookup_chain(m, p, i, j)
if m[i, j] < ∞
return m[i, j]
if i == j

## 第 100 页

algorithm - Memoized version of matrix chain multiplication - Stack Overflow
4. 最长公共子序列
在生物应用中，经常需要比较两个不同生物体的 DNA 。某种生物 DNA 为 S1 ，另一种为 S2 。
如果一个 DNA 串，转化为另一个 DNA 串所需的操作很少，那么可以说两个串是相似的 ( 编辑距
离 ) 。
如果一个 DNA 串是另一个串的子串，可以说是相似的 ( 字符串匹配 ) 。
另一种方式是寻找第三个串 S3 ，它的所有碱基也都出现在 S1 和 S2 中，且在三个串中出现的顺序
都相同，但在 S1 和 S2 中不要求连续出现。可以找到的 S3 越长，就可以认为 S1 和 S2 相似度越高。
这一种方式就是最长公共子序列问题，即 LCS 。
m[i, j] = 0
else for k = i to j - 1
q = Lookup_chain(m, p, i, k)
+ Lookup_chain(m, p, k + 1, j) + pi-1pkpj
if q < m[i, j]
m[i, j] = q
return m[i, j]

## 第 101 页

1. 刻画最长公共子序列的特征
如果用暴力搜索方法求解 LCS 问题，就要穷举 X 的所有子序列，对每个检查它是否也是 Y 的
子序列，记录找到的最长子序列。这个方法无疑是指数复杂度的。
2. 一个递归解
定理 15.1 意味着，在求 X 和 Y 的一个 LCS 时，我们需要求解一个或者两个子问题。如果
xm=yn ，我们应该求解 Xm-1 和 Yn-1 的一个 LCS 。将 xm=yn 追加到这个 LCS 的末尾，就得到 X
和 Y 的一个 LCS 。如果 xm 不等于 yn ，我们必须求解两个子问题：求 Xm-1 和 Y 的一个 LCS 与 X
和 Yn-1 的一个 LCS 。两个 LCS 较长者即为 X 和 Y 的一个 LCS 。由于这些情况覆盖了所有可能
性，因此我们知道必然有一个子问题的最优解出现在 X 和 Y 的 LCS 中。
在递归公式中，我们通过条件限定了需要求解哪些子问题。当 xi=yi 时，求 c[i-1, j-1] +1...
在之前讨论过的钢条切割问题和矩阵乘法问题的动态规划算法中，根据问题的条件，我们没
有排除任何子问题。不过 LCS 问题并非唯一根据条件排除子问题的动态规划算法。例如，编
辑距离问题也具有这样的特点。
3. 计算 LCS 的长度
4. 构造 LCS
LCS 问题只有Θ(mn)个不同的子问题，我们可以使用动态规划。
LCS_Length(X, Y)
m = X.length
n = Y.length
let b[1..m, 1..n] and c[0..m, 0..n] be new tables
for i = 1 to m

## 第 102 页

c[i, 0] = 0
for j = 0 to n
c[0, j] = 0
for i = 1 to m
for j = 1 to n
if xi == yi
c[i, j] = c[i - 1, j - 1] + 1
b[i, j] = "↖"
else if c[i - 1, j] >= c[i, j - 1]
c[i, j] = c[i - 1, j]
b[i, j] = "↑"
else c[i, j] = c[i, j - 1]
b[i, j] = "←"
return c and b
Print_lcs(b, X, i, j)
if i == 0 or j == 0
return
if b[i, j] == "↖"
Print_lcs(b, X, i - 1, j - 1)
print xi
else if b[i, j] == "↑"
Print_lcs(b, X, i - 1, j)
else Print_lcs(b, X, i, j - 1)

## 第 103 页

LCS_Length(X, Y) 时间复杂度为Θ(mn)， Print_lcs 时间复杂度为 O(m + n) 。
算法改进
本例中我们可以改进 LCS 算法，节省空间。即为滚动数组。
5. 最优二叉搜索树
实现从英文文本到法语的翻译。对英文文本中的每个单词，我们需要查找他对应的法语单词。为
了实现这些查找操作，我们可以创建一颗二叉搜索树，将 n 个英语单词作为关键字，对应的法语
单词作为关联数据。假定用红黑树，每次搜索时间为 O(lgn) 。但是单词出现的频率是不同的，常
用的单词可能在叶子结点上，比如 “the” ，不常用的单词可能在根附近。这样会减慢翻译的速
度，因为在二叉搜索树中搜索一个关键字需要访问的结点数等于包含关键字结点的深度加一。我
们希望频繁出现的单词被放置在靠近根的位置。一些单词没有对应法语的，不应该出现在二叉树
中。在给定单词出现频率的前提下，我们应该如何组织一颗二叉搜索树，使得所有搜索访问结点

## 第 104 页

次数最少呢？这个问题为最优二叉搜索树问题。
后面太多分析了，略，具体见书本。
1. 最优二叉搜索树的结构
2. 一个递归算法
3. 计算最优二叉搜索树的期望搜索代价
见书本分析
Optimal_BST(p, q, n)
let e[1..n + 1, 0..n], w[1..n + 1, 0..n] and root[1..n, 1..n] be new
tables
for i = 1 to n + 1 // base cases

## 第 105 页

6.CBMS 动态规划专题
算法导论的内容完结了下面补充一些常见的动态规划问题。
背包问题
石头问题
找零钱问题
曼哈顿游客问题
编辑距离
LCS 问题
e[i, i − 1] = qi−1
w[i, i − 1] = qi−1
for l = 1 to n
for i = 1 to n − l + 1
j = i + l - 1
e[i, j] = ∞
w[i, j] = w[i, j − 1] + pj + qj
for r = i to j
t = e[i, r − 1] + e[r + 1, j] + w[i, j]
if t < e[i, j]
e[i, j] = t
root[i, j] = r
return e and root

## 第 106 页

## 第 107 页

有限状态机

## 第 108 页

Needleman-Wunsch Algorithm (Global Alignment) Pseudocode:
function NeedlemanWunsch(text1, text2):
    n = length(text1)
    m = length(text2)
    f = array of size (n+1, m+1)
    // Initialize the first row and column with gap penalties
    for i from 0 to n:

## 第 109 页

Smith-Waterman Algorithm (Local Alignment) Pseudocode:
        f[i][0] = i * gap_penalty
    for j from 0 to m:
        f[0][j] = j * gap_penalty
    // Fill the matrix
    for i from 1 to n:
        for j from 1 to m:
            match = f[i-1][j-1] + score(text1[i-1], text2[j-1])
            delete = f[i-1][j] + gap_penalty
            insert = f[i][j-1] + gap_penalty
            f[i][j] = max(match, delete, insert)
    // Traceback to find the optimal alignment
    alignment1 = ""
    alignment2 = ""
    i = n
    j = m
    while i > 0 and j > 0:
        if f[i][j] == f[i-1][j-1] + score(text1[i-1], text2[j-1]):
            alignment1 = text1[i-1] + alignment1
            alignment2 = text2[j-1] + alignment2
            i = i - 1
            j = j - 1
        else if f[i][j] == f[i-1][j] + gap_penalty:
            alignment1 = text1[i-1] + alignment1
            alignment2 = "-" + alignment2
            i = i - 1
        else:
            alignment1 = "-" + alignment1
            alignment2 = text2[j-1] + alignment2
            j = j - 1
    return alignment1, alignment2
function SmithWaterman(text1, text2):
    n = length(text1)
    m = length(text2)
    f = array of size (n+1, m+1)
    // Initialize the matrix with zeros
    for i from 0 to n:
        f[i][0] = 0

## 第 110 页

17. 贪心算法 (Greedy Algorithms)
求解最优化问题通常需要经过一系列的步骤，在每个步骤都面临多种选择。对于许多最优化问
题，使用动态规划算法来求最优解有些杀鸡用牛刀了，可以使用更简单，更高效的算法。
贪心算法就是这样的算法，它在每一步都做出当时看起来最佳的选择。也就是说，它总是做出局
部最优的选择，寄希望这样的选择能导致全局最优解。本章介绍一些贪心算法能找到最优解的最
优化问题。
    for j from 0 to m:
        f[0][j] = 0
    // Fill the matrix
    max_score = 0
    max_i, max_j = 0, 0
    for i from 1 to n:
        for j from 1 to m:
            match = f[i-1][j-1] + score(text1[i-1], text2[j-1])
            delete = f[i-1][j] + gap_penalty
            insert = f[i][j-1] + gap_penalty
            f[i][j] = max(0, match, delete, insert)
            if f[i][j] > max_score:
                max_score = f[i][j]
                max_i = i
                max_j = j
    // Traceback to find the optimal local alignment
    alignment1 = ""
    alignment2 = ""
    i = max_i
    j = max_j
    while f[i][j] > 0:
        if f[i][j] == f[i-1][j-1] + score(text1[i-1], text2[j-1]):
            alignment1 = text1[i-1] + alignment1
            alignment2 = text2[j-1] + alignment2
            i = i - 1
            j = j - 1
        else if f[i][j] == f[i-1][j] + gap_penalty:
            alignment1 = text1[i-1] + alignment1
            alignment2 = "-" + alignment2
            i = i - 1
        else:
            alignment1 = "-" + alignment1
            alignment2 = text2[j-1] + alignment2
            j = j - 1
    return alignment1, alignment2

## 第 111 页

贪心算法并不保证得到最优解，但对很多问题确实可以求得最优解。 1 我们介绍活动选择问题，
先考虑用动态规划方法解决这个问题，然后证明一直做贪心选择就可以得到最优解，从而得到一
个贪心算法。 2 会回顾贪心方法的基本要素，并给出一个直接的方法用来证明贪心算法的正确
性。 3 会提出贪心技术的一个重要应用，哈弗曼编码。
贪心算法是一种强有力的算法设计方法。最小生成树算法， Dijkstra 算法，以及集合覆盖问题的
Chvatal 贪心启发式算法。
1. 活动选择问题
一个调度竞争共享资源的多个活动的问题，目标是选出一个最大的互相兼容的活动集合。假设有
n 个活动占用同一个资源 ( 如教室 ) ，同一时间只能提供给一个教室。每个活动都有一个开始时间 si
和一个结束时间 fi 。我们希望选择一个最大兼容活动集。
我们可以通过动态规划方法把这个问题分为两个子问题，然后将两个子问题的最优解整合成原问
题的一个最优解。在确定将哪些子问题用于最优解时，要考虑几种选择。而使用贪心算法，在这
一步只需考虑一个选择 ( 贪心选择 ) ，在做贪心选择时，子问题之一必是空的，因此只留下一个非
空问题。基于这些观察，我们将找到一种递归贪心算法来解决活动调度问题，并将递归算法转化
为迭代算法，以完成贪心方法的过程。
活动选择问题的最优子结构
我们很容易验证活动选择问题具有最优子结构性质。令Sij表示在 ai 结束之后开始，且在 aj 开
始之前结束的那些活动的集合。假定我们希望求Sij的一个最大的互相兼容的活动子集，进
一步假定Aij就是这样一个子集，包含活动 ak 。由于最优解包含活动 ak ，我们得到两个子问
题，寻找Sik中的兼容活动和Skj中的兼容活动。

## 第 112 页

递归贪心算法
为了方案算法初始化，我们添加一个虚拟活动a0，其结束时间f0=0 ，这样子问题S0就是完整的活
动集S0。求解原问题即可调用 Recursive_activity_selector(s, f, 0, n) 。
这样刻画活动选择问题的最优子结构，意味着我们可以用动态规划方法求解活动选择问题。
贪心选择
假如我们无需求解所有子问题就可以选择出一个活动加入到最优解，这将使我们省去递归式
中固有的考察所有选择的过程，事实上，对于活动选择问题，我们只需要考虑贪心选择。
直观上，我们应该选择这样的一个活动，选出它后剩下的资源应能够被尽量多的其他任务所
用。现在考虑可选的活动，其中必然有一个最先结束。直觉告诉我们，应该选择 S 中最早结
束的活动，因为它剩下的资源可供它之后尽量多的活动使用。由于活动已按照结束时间单调
递增的顺序排序，贪心选择就是活动 a1 。选择最早结束的活动并不是本问题唯一的贪心选择
方法。
Theorem 16.1
Consider any nonempty subproblem Sk, and let am be an activity in Sk with the earliest
finish time. Then am is included in some maximum-size subset of mutually compatible
activities of Sk.
Proof...
Recursize_activity_selector(s, f, k, n)
m = k + 1
while m <= n and s[m] < f[k] //find the first activity in Sk to
finish
m ++;

## 第 113 页

下图为算法对前文例子的执行过程。
最终结果为 {a1, a4, a8, a11} 。
if m <= n
return {am} ∪ Recursize_activity_selector(s, f, m, n)
else return ∅

## 第 114 页

假定活动已经按结束时间排好序，则递归调用 Recursive_activity_selector 的运行时间为Θ(n)，我
们稍后证明这个结论。在整个递归调用过程中，每个活动被且只被第二行的 while 循环检查一次。
特别地，活动 ai 在 k<i 的最后一次调用中被检查。
迭代贪心算法
下面代码假定活动已经按照结束时间单调递增顺序排好序。它将选出的活动存入集合 A 中，并将 A
返回调用者。
2. 贪心算法原理
贪心算法通过做出一系列选择来求出问题的最优解。在每个决策点，它做出在当时看来最佳的选
择。这种启发式策略并不保证总能找到最优解，但对有些问题确实有效，如活动选择问题。接下
来讨论一下贪心算法的一些一般的性质。
在活动选择问题中，我们设计贪心算法的过程比通常的繁琐很多，主要是为了理解。
Greedy_activity_selector(s, f)
n = s.length
A = {a1}
k = 1
for m = 2 to n
if s[m] >= f[k] //is am in Sk?
A = A ∪ {am} //yes, so choose it
k = m //and continue from there
return A
确定问题的最优子结构
设计一个递归算法
证明如果我们做出一个贪心选择，则只剩下一个子问题
证明贪心选择总是安全的

## 第 115 页

那么我们如何能证明一个贪心算法是否能求解一个最优化问题呢？并没有适合所有情况的方法，
但贪心选择性质和最优子结构是两个关键要素。如果我们能够证明问题具有这些性质，就向贪心
算法迈出了重要的一步。
贪心选择性质
第一个关键要素是贪心选择性质，我们可以通过做出局部最优选择来构造全局最优解。换句话
说，当进行选择时，我们直接做出在当前问题中看来最优的选择，而不必考虑子问题的解。与动
态规划先求解子问题才能进行第一次选择不同，贪心算法在进行第一次选择之前不求解任何子问
题。一个动态规划算法是自底向上进行计算的，而一个贪心算法通常是自顶向下的，进行一次又
一次的选择，将给定问题的实例变得更小。当然，我们必须证明每个步骤做出贪心选择能生成全
局最优解。
最优子结构
如果一个问题的最优解包含其子问题的最优解，则称此问题具有最优子结构性质。
当应用于贪心算法时，我们通常使用更为直接的最优子结构。我们可以假定，通过对原问题应用
贪心选择即可得到子问题。我们真正要做的全部工作就是论证：将子问题的最优解与贪心选择组
合在一起就能生成原问题的最优解。这种方法隐含地对子问题使用了数学归纳法，证明了在每个
步骤进行贪心选择会生成原问题的最优解。
贪心对动态规划
由于贪心和动态规划都用了最优子结构，你可能会对一个可用贪心算法求解的问题设计一个动态
规划算法，或者相反，对一个实际上需要用动态规划求解的问题使用了贪心方法。为了说明两种
方法的区别，请看下面问题。
0-1 背包问题： ...
设计一个递归算法实现贪心策略
将递归算法改为迭代算法
将最优化问题转化为这样的形式：对其做出一次选择后，只剩下一个子问题需要求解
证明做出贪心选择后，原问题总是存在最优解，即贪心选择总是安全的
证明做出贪心选择后，剩余的子问题满足性质：其最优解与贪心选择组合即可得到原问题的
最优解，这样就得到了最优子结构。
我们将使用这种简便的设计思路。但我们应该知道，在每个贪心算法之下，几乎总有一个更
繁琐的动态规划算法。

## 第 116 页

分组背包问题：对每个商品，小偷可以拿走其中一部分，而不是只能做出二元选择。你可以将 0-
1 背包问题中的商品想象为金锭，而分数背包问题中的商品更像金沙。
两个问题都有最优子结构。但我们用贪心策略可以求解分数背包问题，而不能求解 0-1 背包问
题。
3. 赫夫曼编码
赫夫曼编码可以很有效地压缩数据：通常可以节省 20%~90% 的空间，具体压缩率依赖于数据的
特性。我们将待压缩数据看做字符序列。根据每个字符的出现评率，赫夫曼贪心算法构造出字符
的最优二进制表示。

## 第 117 页

HUFFMAN(C)
n = |C|
Q = C
for i = 1 to n - 1
allocate a new node z
z.left = x = Extract_min(Q)
z.right = y = Extract_min(Q)
x.freq = x.freq + y.freq
INSERT(Q, z)
return Extract_min(Q) //the root of the tree is the only node left

## 第 118 页

赫夫曼算法的正确性 ...
18. 摊还分析 (Amortized Analysis)
19.B 树 (B-Trees)
B 树是为磁盘或者其他直接存取的辅助存储设备而设计的一种平衡搜索树。 B 树类似于红黑树，但
它们在降低磁盘 I/O 操作数方面要更好一些。许多数据库系统使用 B 树或者 B 树的变种来存储信
息。
与红黑树不同之处在于 B 树的结点可以有很多孩子。每颗含有 n 个结点的 B 树的高度为 O(lgn) ，一
颗 B 树的严格高度可能比一颗红黑树的高度要小许多，这是因为它的分支因子，也就是表示高度
的对数的底数可以非常大。

## 第 119 页

如果 B 树的一个内部结点 x 包含 x.n 个关键字，那么结点 x 就有 x.n + 1 个孩子。结点 x 中的关键字就
是分隔点，它把结点 x 中所处理的关键字的属性分隔为 x.n + 1 个子域，每个子域都由 x 的一个孩子
处理。当在一颗 B 树查找一个关键字时，基于对存储在 x 中的 x.n 个关键字比较，做出一个 (x ， n +
1) 路的选择。叶结点的结构与内部结点不同。
1 给出 B 树定义，证明 B 树的高度仅随他所包含的结点数按对数增长。 2 介绍如何在 B 树中查找和插
入一个关键字， 3 讨论删除操作。
//B 树查找
B_tree_search(x, k)
i = 1
while i <= x.n and k > x.keyi
i ++;
if i <= x.n and k == x.keyi
return(x, i)
else if x.leaf
return NIL
else Disk_read(x, ci)
return B_tree_search(x.ci, k)
// 创建 B 树
B_tree_create(T
x = Allocate_node()
x.leaf = True
x.n =
Disk_write(x
t.root = x
// 分裂叶子结点
B_tree_split_child(x, i)
z = Allocate_node()
y = x.ci
z.leaf = y.leaf
z.n = t - 1
for j = 1 to t - 1
z.key_j = y.key_j+t
if not y.leaf
for j = 1 to t

## 第 120 页

20. 斐波那契堆 (Fibonacci Heaps)
21. 并查集 (Disjoint Sets)
一些应用涉及到将 n 个不同的元素分成一组不相交的集合。这些应用经常需要进行两种特别的操
作：寻找包含给定元素的唯一集合和合并两个集合。
并查集维护了一个不相交动态集的集合 S 。我们用一个代表来标识每个集合，它是这个集合的某
个成员。我们希望并查集实现如下操作 :
Make_set(x) ：建立一个新的集合，它的唯一成员是 x 。因为各个集合是不相交的，故 x 不会出现
在别的某个集合中。
Union(x ， y) ：将包含 x 和 y 的两个动态集合合并成一个新的集合，即这两个集合的并集。假定在操
作之前这两个集合是不相交的。
Find_set(x) ：返回一个指针，这个指针指向包含 x 的集合的代表。
z.cj = y.c_j+t
y.n = t - 1
for j = x.n + 1 downto i +1
x.c_j+1 = x.cj
x.c_i+1 = z
for j = x.n downto i
x.key_j+1 = x.keyj
x.keyi = y.keyt
x.n = x.n + 1
Disk_write(y)
Disk_write(z)
Disk_write(x)
// 向 B 树插入关键字
B_tree_insert(T, k)
r = T.root
if r.n == 2t - 1
s = Allocate_node()
T.root = s
s.leaf = FALSE
s.n = 0
s.c1 = r
B_tree_split_child(s, 1)
B_tree_insert_nonfull(s, k)
else B_tree_insert_nonfull(r, k)
B_tree_insert_nonfull(x, k)

## 第 121 页

并查集的一个经典应用是确定无向图的连通分量。
Same_component 回答两个顶点是否在同一个连通分量的访问。
不相交集合的链表表示
可以用链表来表示。每个集合用一个自己的链表。都有 head 和 tail 属性。 head 指向表的第一个对
象， tail 指向表的最后一个对象。链表中的每个对象都包含一个集合成员，一个指向链表中下一个
对象的指针和一个指回到集合对象的指针。
Connected_components(G)
for each vextex v ∈ G.V
Make_set(v)
for each edge(u, v) ∈ G.E
if Find_set(u) != Find_set(v)
Union(u, v)
Same_component(u, v)
if Find_set(u) == Find_set(v)
return True
else return False

## 第 122 页

## 第 123 页

不相交集合森林
Make_set 操作简单地创建一颗只有一个结点的树， Find_set 操作通过沿着指向父节点的指针找到
树的根。 Union 操作使得一棵树的根指向另外一棵树的根。
改进运行时间的启发式策略
按秩合拼 (union by rank)：类似链表表示中使用的加权合并启发式策略。显而易见的做法
是，使具有较小结点的树的根指向具有较多的树的根。这里，对于每个结点，维护一个秩，

## 第 124 页

它表示该节点高度的一个上界。让具有较小的秩的根指向具有较大秩的根。
路径压缩 (path compression)：如图。
对每个结点 x ，维护一个整数值 x.rank 。 Union 操作有两种情况，取决于两颗树的根是否有相
同的秩。如果根没有相同的秩时，较大的秩成为父节点，秩不变。如果秩相同，就任意选一
个为父节点，然后秩 +1 。
Make_set(x)
x.p = x
x.rank = 0
Union(x, y)
Link(Find_set(x), Find_set(y))
Link(x, y)
if x.rank > y.rank
y.p = x
else x.p = y
if x.rank == y.rank
y.rank = y.rank + 1
Find_set(x)
if x != x.p
x.p = Find_set(x.p)
return x.p

## 第 125 页

又是证明该死的。
22. 基本图算法
本章主要是图的表示和图的搜索。图的搜索指的是系统化地跟随图中的边来访问图中的每个结
点。
图的搜索算法可以用来发现图的结构。许多图算法在一开始都会先通过搜索来获得图的结构，其
他的一些图算法则是对基本的搜索加以优化。图的搜索技巧是整个图算法的核心。
1.BFS
广度优先搜索是最简单的图搜索算法之一，也是许多重要的图算法的原型。Prim的最小生成树算
法，Dijkstra的单元最短路路径算法，都用了广度优先搜索的思想。
BFS 的伪代码： BFS 与 DFS 我在之前的文章已经详细介绍过了， BFS 和 DFS 是图论算法的基础，
这里写一下他们的伪代码。强烈建议记住 CLRS 上的伪代码并且熟练到会默写的程度。
BFS(G, s)
    for each vertex u in G.V - {s}
        u.color = WHITE
        u.d = ∞
        u.parent = NIL
    s.color = GRAY
    s.d = 0
    s.parent = NIL
    Q = null
    enqueue(Q, s)
    while Q != null
        u = dequeue(Q)

## 第 126 页

白色的点是还没有被我们发现的点，灰色的点是被我们发现的点，黑色的点是我们已经完成了该
点的领边探索。此算法的时间复杂度为 O(V + E)，有向图中为 1E ，无向图中为 2E 。每个结点最多
入队一次，出队一次，邻接表中的边最多被扫描一次。
BFS 可以求从给定原结点 s 到所有可以到达的结点的距离。广度优先搜索可以正确的计算出最短
路径。
BFS 有效性的证明：
广度优先树： BFS 过程在图上运行时，将创建一颗广度优先树。我们可以用下面的伪代码打印出
从源节点 s 到结点 v 的一条最短路径上的所有结点，这里假设 BFS 已经计算出一颗广度优先树。
        for each v in G.Adj[u]
            if v.color == WHITE
                v.color = GRAY
                v.d = u.d + 1;
                v.parent = u
                enqueue(Q, v)
        u.color = BLACK

## 第 127 页

每次调用时的路径都比前一次调用的路径少一个结点，所以该过程的运行时间是关于所输出路径
顶点数的一个线性函数。
Application:
2.DFS
DFS 是一种简单的算法，但是可以做到很多事情，大部分算法都需要用到 DFS ，因此 DFS 是各种
面试和笔试的常客，与 BFS 不同， DFS 在图上运行会形成深度优先森林。算法导论中伪代码与网
上给出的朴素版有所区别， CLRS 中喜欢存储时间戳等信息，这些信息通常能推断深度优先搜索
算法的行为。
Print_path(G, s, v)
if v == s
print s
else if v.parent == NIL
print"no path from" s "to" v "exists"
else Print_path(G, s, v.parent)
print v
web crawling(how Googel finds pages)
social networking(Facebook friend finder)
network broadcast routing
garbage collection
model checking(finite state machine)
checking mathmatical conjectures
solving puzzles & games
Dfs(G):
for each vertex u ∈ G.V
u.color = WHITE
u.parent = NIL
time = 0
for each vertex u ∈ G.V
if u.color == WHITE
Dfs_visit(G, u)
Dfs_visit(G, u)
time ++ // white vertex u has just been discovered
u.d = time
u.color = GRAY
for each vertex v in G.Adj[u] // explore each edge(u, v)
if v.color = WHITE

## 第 128 页

当 DFS 算法完成时，每个结点 u 都被赋予一个发现时间 u.d 和一个完成时间 u.f 。所有的结点都被染
成黑色。 DFS 的时间复杂度为 O(V + E)
DFS 的性质: 生成的前驱子图形成一个由多棵树所构成的森林。结点 v 是深度优先森林里结点 u 的
后代当且仅当结点 v 在结点 u 为灰色的时间段里被发现。
另一个重要的性质是结点的发现时间和完成时间具有 parenthesis structure 当我们使用 DFS 遍
历图时，每个节点都有一个发现时间和一个完成时间。我们可以用 “(u” 表示节点 u 的发现，用 “u)”
表示节点 u 的完成。所有这些括号会适当地嵌套在一起，形成一种括号化结构。
v.parent = u
Dfs_visit(G, v)
time ++
u.f = time
u.color = BLACK

## 第 129 页

Theorem22.7（括号定理）：在对有向或无向图 G = (V, E) 进行的任意深度优先搜索中，对于任
意两个结点 u 和 v 来说，下面三种情况只有一种成立：
Theorem22.8（后代区间的嵌套）在有向图或无向图 G 的深度优先森林中，结点 v 是结点 u 的真后
代当且仅当 u.d < v.d < v.f < u.f 成立。
Theorem22.9（白色路径定理）在有向或无向图 G = (V, E) 的深度优先森林中，结点 v 是结点 u 的
后代当且仅当在发现结点 u 的时间 u.d ，存在一条从结点 u 到结点 v 的全部由白色结点所构成的路
径。
边的分类：深度优先搜索可以对图中的边进行分类，每条边的类型可以提供关于图的重要信息。
区间 [u.d, u.f] 和区间 [v.d, v.f] 完全分离，在深度优先森林中，结点 u 不是结点 v 的后代，结点 v
也不是结点 u 的后代。
区间 [u.d, u.f] 完全包含在区间 [v.d, v.f] 内，在深度优先森林中，结点 u 是结点 v 的后代。
区间 [v.d, v.f] 完全包含在区间 [u.d, u.f] 内，在深度优先森林中，结点 v 是结点 u 的后代。
树边 tree edge ：深度优先森林 G 中的边。如果结点 v 搜索因算法对边 (u, v) 的探索而首先被发
现，则 (u, v) 是一条树边。
后向边 backward edge ：后向边 (u, v) 是将结点 u 连接到其在深度优先树中一个祖先结点 v 的
边。有向图中可以有自循环，故自循环也被认为是后向边。
前向边 forward edge ：是将结点 u 连接到其在深度优先树中一个后代结点 v 的边 (u, v) 。
横向边 cross edge ：指其他的所有边。这些边可以连接同一颗深度优先树中的结点，只要其
中一个结点不是另外一个结点的祖先，也可以连接不同深度优先树中的两个结点。

## 第 130 页

Theorem22.10 在对无向图 G 进行深度优先搜索时，每条边要么是树边，要么是后向边。
DFS 的一个经典应用是Cycle detection:
拓扑、 dfs 、并查集都可以图中判断有没有环， floyd 可以找最小环 .
Theorem22.11 一个有向图 G = (V, E) 是无环的当且仅当对其进行的深度优先搜索不产生后向边。
Proof: G has a cycle <=>DFS has a back edge.
<=: Suppose that there are back edges, then in DFS forest, vertex v is the ancestor of u.
Therefore, G has a path from v to u. This path connected with backward edge (u, v) form a
cycle together.
=>: if there is a cycle. Suppose v is the first vertex discovered in cycle(c), set (u, v) be an edge
in front of node v in loop c. In the moment of v.d, all the vertex in cycle(c) form a path from v to
u with all the vertex showing white color. According to Theorem 22.9, node u must be the
descendant of v. Therefore, (u, v) is a backward edge.
各种边的示意图
DFS 有足够的信息来对这些边进行分类。这里的关键是，当第一次探索边 (u, v) 时，结点 v 的
颜色能够告诉我们关于该条边的一个信息。
结点 v 为白色表明该条边 (u, v) 是一条树边。
结点 v 为灰色表明该条边 (u, v) 是一条后向边。
结点 v 为黑色表明该条边 (u, v) 是一条前向边或横向边，边 (u, v) 在 u.d <v.d 时为前向边，在
u.d > v.d 时为横向边。证明。

## 第 131 页

DFS 的另一个经典应用是Topological sort:
上面伪代码是我根据算法导论改编了一下，原文描述为：
//dfs 实现拓扑排序
Topological_sort(G):
initialize an empty linked list
Dfs_sort(G)
return the linked list
Dfs_sort(G):
for each vertex u in G:
if u in not visited:
Dfs_visit(u)
Dfs_visit(u):
mark u as visited
for each v in G.Adj[u]:
if v is not visited:
Dfs_visit(v)
insert u at the front of the linked list
Topological_sort(G):
call DFS to compute cache finishing time v.f for each vertex v
as each vertex is finished, insert it onto the front of a listed
list.
return the linked list of vertices
// 这里有另外一种方法写拓扑排序，根据图中点的度数，度数为 0 的点作为起点入队， while 循环队列非
空，取出队头，遍历这个点的所有临边，删除这些边，并且这些点的度数减一， if 判断 d[j] 是否为 0 ，

## 第 132 页

拓扑排序的时间复杂度是 O(V + E)，调用 DFS ，时间复杂度与其相同。
Theorem22.12 拓扑排序算法生成的是有向无环图的拓扑排序。
Proof: Run DFS in DAG, we just need to proof that for every diffierent nodes u, v in V, if we
have a line from u to v, we get v.f < u. f. Now consider any edge(u, v) explored by the algorithm,
the node v cant be GRAY, because in that case v will be the ancestor of u, (u,v) must be a
backward edge contradicting with Theorem22.11. So v is either WHITE or BLACK. Suppose v is
WHITE, it will become the decendent of u, therefore v.f < u.f. Suppose v is BLACK, v.f has
already been done, but u.f is still being added. Once u.f is over, u.f must bigger than v.f, v.f < u.f.
In conclusion, we alway have v.f < u.f.
DFS 的另另一个经典应用是Strongly Connected Components:
将有向图分解为强连通分量。有向图 G = (V, E) 的强连通分量是一个最大结点集合 C in V ， 对于
该集合中的任意一对结点 u 和 v 来说，路径 u 到 v 和路径 v 到 u 同时存在，也就是说，结点 u 和结点 v 可
以相互到达。
如果为 0 就入队。这种方法是用 BFS 实现。
Topo_sort(G):
for each v in G:
if(indeg(v) == 0)
enqueue(Q, v)
while Q != null
int u = dequeue(Q)
insert u to the end of List.
for each v in G.Adj[u]:
int j = v;
indeg(i) --
if(indeg(j) == 0)
enqueue(Q, v)
return List.length == G(V).size

## 第 133 页

有向图和他的转置和压缩图
寻找强连通分量的算法需要用到图 G = (V, E) 的转置， GT = (V, ET) ，这里 ET = { （ u, v) : (v, u) in
E} 。也就是说 ET 对图 G 中的边进行反向而获得。给定 G 的邻接链表，创建 GT 的时间为 O(V + E) 。
图 G 和图 GT 的强连通分量完全相同： u 和 v 在图 G 中可以互相到达当且仅当他们在图 GT 中可以相
互到达。下面算法使用两次深度优先搜索来计算有向图 G 的强连通分量。这两次深度优先搜索一
次运行在图 G 上，一次运行在转置图 GT 上。
这种算法叫 Kosaraju 算法，时间复杂度为 O(V + E) 。
过于简洁 我们来修改一下 QAQ 以便符合各种面试和考试：
Scc(G):
call DFS(G) to compute finishing time u.f for each vertex u.
compute GT
call DFS(GT), but in the main loop of DFS, consider the vertices in
order of decreasing u.f
output the vertices of each tree in the depth-first forest formed in
line 3 as a separate strongly connected component
Dfs1(G):
time = 0
for each vertex u in G:
if u is not visited:
Dfs_visit1(G, u)
Dfs_visit1(G, u):
time ++
mark u as visited
for each v in adj[u]:
if v is not visited:
Dfs_visit1(G, v)
time ++
u.f = time
push u onto stack in increasing order of the finishing time
reverse_GT(G):
GT = new graph
for each u in G:
for each v in adj[u]:
add edge(v, u) to GT
return GT
Dfs2(GT):
while stack is not empty:
u = pop from stack

## 第 134 页

分量图的关键性质是：分量图是一个有向无环图。该事实可由下面的引理所推出。
Theorem22.13 设 C 和 C' 为有向图 G=(V, E) 的两个不同的强连通分量，设结点 u ， v 属于 C ，结点
u' ， v' 属于 C' ，假定图 G 包含一条从 u 到 u' 的路径。那么 G 不可能包含一条从 v' 到 v 的路径。
d(U) 和 f(U) 分别是结点集合 U 中所有结点里最早的发现时间和最晚的完成时间。
Theorem22.14 设 C 和 C' 为有向图 G=(V, E) 的两个不同的强连通分量。假如存在一条边 (u, v) 是属
于 E ，这里 u 属于 C ， v 属于 C' ，则 f(C) > f(C') 。
Theorem22.15 设 C 和 C' 为有向图 G=(V, E) 的两个不同的强连通分量，假如存在一条边 (u, v) 属于
ET ，这里 u 属于 C ， v 属于 C' ，则 f(C) > f(C') 。
Theorem22.16 算法 SCC 能够正确计算出有向图 G 的强连通分量。
23. 最小生成树 (MST)
Minimum Spanning Tree 主要有两种算法： Kruskal 算法和 Prim 算法。如果使用普通的二叉堆，那
么可以很容易地将这两个算法的时间复杂度限制在O(ElgV)，如果使用斐波那契堆， Prim 算法的
运行时间将改善为O(E + VlgV)。此运行时间在 V 远远小于 E 的情况下较二叉堆有很大改进。这两
种最小生成树算法都是贪心算法。
最小生成树的形成：假设一个连通无向图 G=(V, E) 和权重函数 w ，我们希望找出图 G 的一颗最小生
成树。本文讨论的两种算法都使用贪心策略来解决这个问题，但是它们使用贪心策略的方式却有
不同。下面的方法可以来表述算法的贪心策略。该方法在每个时刻生长最小生成树的一条边，并
在整个策略的实施过程中，管理一个遵守下述循环不变式的边集合 A 。
在每遍循环之前， A 是某棵最小生成树的一个子集。
在每一步我们要做的事情是选择一条边 (u, v) ，将其加入到集合 A 中，使得 A 不违反循环不变式，
即 A ∪ (u, v) 也是某棵最小生成树的子集。由于我们可以安全的将这种边加入到集合 A 而不会破坏 A
的循环不变式，因此称这样的边为集合 A 的安全边。
SCCs = [][]
if u is not visited:
SCC = new list
Dfs_visit2(GT, u, SCC)
SSCs.append(SCC)
Dfs_visit2(GT, u, SCC):
mark u as visited
SCC.append(u)
for each v in GT.adj[u]:
if v is not visited:
Dfs_visit2(GT, v, SCC)

## 第 135 页

这里的奥妙是算法的第 4 行，找到一条安全边。这条安全边必然存在，因为在执行算法第 4 行时，
循环不变式告诉我们存在一颗生成树 T ，满足 A 属于 T 。在 3 到 5 行的 while 循环体内，集合 A 一定是
T 的真子集，因此，必然存在一条边 (u, v) 属于 T ，使得 (u, v) 不属于 A ，并且 (u, v) 对于集合 A 是安
全的。
用来辨认安全边的规则由下面的定理给出：
Theorem23.1 设 G=(V, E) 是一个在边 E 上定义了实数值权重函数 w 的连通无向图。设集合 A 为 E 的
一个子集，且 A 包括在图 G 的某棵最小生成树中，设 (S, V - S) 是图 G 中尊重集合 A 的任意一个切
割，又设 (u, v) 是横跨切割 (S, V - S) 的一条轻量级边。那么边 (u, v) 对于集合 A 是安全的。
Generic_mst 算法的第 2~4 行的 while 循环执行的总次数为 |V| - 1 次，因为该循环的每遍循环都找出
最小生成树所需 |V| - 1 条边的一条。求最小生成树的两个算法将使用下面的推论。
Deduction23.2 设 G=(V, E) 是一个连通无向图，并有定义在边集合 E 上的实数值权重函数 w 。设集
合 A 为 E 的一个子集，且该子集包括在 G 的某课最小生成树里。并设 C=(Vc, Ec) 为森林 Ga=(V, A) 中
的一个连通分量（树）。如果边 (u, v) 是连接 C 和 Ga 中某个其他连通分量的一条轻量级边，则边
(u, v) 对于集合 A 是安全的。
在 Kruskal 算法中，集合 A 是一个森林，其结点就是给定图的结点。每次加入到集合 A 中的安全边
永远是权重最小的连接两个不同分量的边。在 Prim 算法里，集合 A 则是一棵树。每次加入到 A 中
的安全边永远是连接 A 和 A 之外某个结点的边中权重最小的边。
Krukal 算法 :
Krukal 算法适用于稀疏图，使用并查集。
Generic_MST(G, w)
A = null
while A does not form a spanning tree
find an edge (u, v) that is safe for A
A = A ∪ {(u, v)}
return A
MST_kruskal(G, w)
A = null
for each vertex v in G.V
Make_set(v)
sort the edges of G.E into nondecreasing order by weight w
for each e in G.E, taken in nondecreasing order by weight
if Find_set(u) != Find_set(v)
A = A ∪ (u, v)
Union(u, v)
return A

## 第 136 页

对于图 G ， Kruskal 算法的运行时间依赖于不相交集合数据结构的实现方式。假定使用并查集，并
增加按秩合并和路径压缩的功能。对边进行排序需要时间 O(ElgE) 。第 5~8 行时间复杂度为
O(Eα(V)) ， 2~3 行时间复杂度为 O(Vα(V)) 。Kruskal 算法时间复杂度为 O(ElgV) 或者 O(ElgE)。
正确性证明 ...
Prim 算法： Prim 算法的工作原理与 Dijkstra 的最短路径算法相似。Prim 算法所具有的一个性质是
集合 A 中的边总是构成一棵树。算法每一步在连接集合 A 和 A 之外的结点的所有边中，选择一条轻
量级边加入到 A 中。稠密图最好使用 Prim 算法。
使用优先队列实现或者斐波那契堆。
MST_prim(G, w, r)
for each u in G.V
u.key = ∞
u.parent = NIL
r.key = 0
Q = G.V
while Q != null

## 第 137 页

r 为最小生成树的根结点。在算法的执行过程中，所有不在树 A 中的结点都存放在一个基于 key 属
性的最小优先队列 Q 中。对于每个结点，属性 v.key 保存的是连接 v 和树中结点的所有边中最小边
的权重。我们约定，如果不存在这样的边，则 v.key 为正无穷。
Prim 算法的运行时间取决于最小优先队列 Q 的实现方式。如果 Q 是二叉最小优先队列。Prim 算法
的总时间代价为 O(VlgV + ElgV) = O(ElgV)。它与 Kruskal 算法的运行时间相同。如果是用斐波那
契堆来实现 Q ，则Prim 算法的运行时间将改进到 O(E + VlgV)。
function FindSMST(graph, mst):
Input: graph (a list of edges with weights), mst (minimum spanning tree as a list of edges)
Output: smst_weight (weight of the second minimum spanning tree)
function FindMaxEdgeInPath(mst, u, v):
Input: mst (minimum spanning tree as a list of edges), u (start vertex), v (end vertex)
Output: max_edge (the edge with the maximum weight in the path from u to v)
u = extract_min(Q)
for each v in Adj[u]
if v in Q and w(u, v) < v.key
v.parent = u
v.key = w(u, v)
smst_weight = infinity
for each edge e in graph - mst:
    // Find the maximum weight edge in the path from u to v in the current
MST
    max_edge = FindMaxEdgeInPath(mst, e.u, e.v)

    // Calculate the weight of the new tree if e replaces max_edge
    new_weight = mst_weight - max_edge.weight + e.weight

    // Update the smst_weight if the new weight is smaller
    if new_weight < smst_weight:
        smst_weight = new_weight
return smst_weight

## 第 138 页

function DFSFindMaxEdge(mst, current, target, visited):
Input: mst (minimum spanning tree as a list of edges), current (current vertex), target (target
vertex), visited (set of visited vertices)
Output: max_edge (the edge with the maximum weight in the path)
24. 单源最短路径 (Single-Source Shortest Paths)
单源最短路问题可以解决其他许多问题，如下面变体：
单目的地最短路径问题：找到从每个结点 v 到给定目的地结点 t 的最短路径。如果我们反向走的
话，就是求其他所有点到给定结点的最短路径。
单结点对最短路问题：对于每对结点 u 和 v ，找到从结点 u 到结点 v 的最短路径。
// Use Depth-First Search (DFS) to find the path from u to v and track the
maximum weight edge
visited = set()
return DFSFindMaxEdge(mst, u, v, visited)
if current == target:
    return Edge(-1, -1, -infinity)  // Placeholder for edge with minimum
weight
visited.add(current)
for edge in mst:
    if edge.u == current and edge.v not in visited:
        next_vertex = edge.v
    elif edge.v == current and edge.u not in visited:
        next_vertex = edge.u
    else:
        continue
    max_edge_in_subpath = DFSFindMaxEdge(mst, next_vertex, target, visited)
    if max_edge_in_subpath.weight > edge.weight:
        max_edge = max_edge_in_subpath
    else:
        max_edge = edge
return max_edge

## 第 139 页

所有结点对最短路径问题：对于每对结点 u 和 v ，找到从结点 u 到结点 v 的最短路径。
最短路径的最优子结构：最短路算法通常依赖的一个重要的性质是两点之间的一条最短路径包含
着其他的最短路径。最优子结构是可以使用动态规划和贪心算法的一个重要指标。 Dijkstra 算法就
是一个贪心算法，而 Floyd-Warshall 算法则是基于动态规划。
Theorem24.1 最短路径的子路径也是最短路径
松弛操作：本章的算法需要使用 relaxation 技术。
Bellman-Ford 算法
Initialize(G, s)
    for each vertex v in G.V
        v.d = ∞
        v.parent = NIL
    s.d = 0
Relax(u, v, w)
    if v.d > u.d + w(u, v)
        v.d = u.d + w(u, v)
        v.parent = u
Bellman_ford(G, w, s)
initialize(G, s)
for i = 1 to |G.V| - 1
for each edge(u, v) ∈ G.E
Relax(u, v, w)
for each edge(u, v) ∈ G.E
if v.d > u.d + w(u.v)
return False
return True

## 第 140 页

Bellman-Ford 算法的运行时间是O(VE)， False 代表有负环， True 表达没有负环。
有向无环图中的单源最短路径问题
DAG_shortest_paths(G, w, s)
Topo_sort(G)
Initialize(G, s)
for each vertex u, taken in topo sorted order
for each v ∈ G.Adj[u]
Relax(u, v, w)

## 第 141 页

算法第 1 行的 topo 排序时间为Θ(V+E)，第 2 行对 Initialize 调用所需的时间为Θ(V)。第 3-5 行的 for
循环对每个结点执行一遍，因此，第 4-5 行的内循环对每条边刚好松弛一次。因为内循环每次运
行时间为Θ(1)( 聚合分析 ) ，算法总运行时间为Θ(V+E)。
证明 ......
Dijkstra 算法
Dijkstra 算法在运行过程中维持的关键信息是一组结点集合 S。从源结点 s 到该集合中每个结点之
间的最短路径已经被找到。算法重复从结点集 V - S 中选择最短路径估计最小的结点 u ，将 u 加入到
集合 S ，然后对所有从 u 发出的边进行松弛。
Dijkstra(G, w, s)
Initialize(G, s)
S = ∅

## 第 142 页

注意到在实现 Dijkstra 算法的过程中使用了优先队列。 Dijkstra 的算法实现时间取决于优先队列 Q
的实现，第一种是朴素版做法，利用结点的编号 1~|V| 来维护最小优先队列。在这种情况下每次
Extract_min 需要扫描一遍所有的结点，算法的总时间为O(V²+E) =O(V²)。如果我们讨论稀疏
图，可以使用二叉堆来实现最小优先队列，从而改变算法的运行时间。 Extract_min 的时间为
log(V)，每次 Decrease_key 执行时间为log(V)，最多有 E 次这样的操作。总时间复杂度为
O((V+E)lgV)。若所有的结点都可以从源点到达则时间为O(ElgV)。而用斐波那契堆来实现最
小优先队列可以把时间复杂度改善到O(VlgV+E)。
25. 所有结点对的最短路径 (All-Pairs Shortest)
我们可以很容易想到对单源最短路问题运行 V 次，这样可以求解所有结点对的最短路径。但是这
样做的话时间复杂度太高。本章试图用不同的方法来解决。本章的多数算法使用邻接矩阵。
如果 Gπ,i 是一棵最短路径树，则下面的过程将打印出从结点 i 到结点 j 的一条最短路径。该算法是
第 22 章的 Print-path 过程的一种修改版本。
Q = G.V
While Q != ∅
u = Extract_min(Q)
S = S ∪ {u}
for each vertex v ∈ G.Adj[u]
Relax(u, v, w)

## 第 143 页

1 讨论如何基于矩阵乘法的动态规划算法来解决所有结点对最短路径问题。如果使用 “ 重复平方 ” 技
术，算法的运行时间为Θ(V3lgV)。 2 给出另一种动态规划算法，即为 Floyd-warshall 算法。该算法
的运行时间为Θ(V3)。 3 讨论 Johnson 算法，该算法能够在O(V2lgV+VE)。
1. 最短路径与矩阵乘法
Print_all_pairs_shortest_path(Matrix, i, j)
if i == j
print i
else if πij = NIL
print"no path from"i"to"j"exists"
else Print_all_pairs_shortest_path(Matrix, i, πij)
print j

## 第 144 页

## 第 145 页

算法 FASTER-ALL-PAIRS-SHORTEST-PATHS(W) 的运行时间为Θ(n3lgn)。
2.Floyd-Warshall 算法
floyd 算法利用动态规划原理。
下面为伪代码：

## 第 146 页

## 第 147 页

构建一条最短路径

## 第 148 页

有向图的传递闭包
3.johnson 算法
Johnson 算法可以在O(V2lgV+VE)内找到所有结点对之间的最短距离。
Johnson 算法使用的技术称为重新赋予权重。 Johnson 算法在运行中需要使用 Dijkstra 算法和

## 第 149 页

Bellman-ford 算法作为自己的子程序。
26. 最大流 (Maximum Flow)
1. 网络流相关定义与证明
JOHNSON(G, w)
compute G‘, where G'.V = G.V ∪ {s}
G'.E = G.E ∪ {(s, v):v ∈ G.V}, and
w(s, v) = 0 for all v ∈ G.V
if Bellman_Ford(G', w, s) == FALSE
print "the input graph contains a negative-weight cycle"
else for each vertex v ∈ G'.V
set h(v) to the value of δ(s, v)
computed by the Bellman-Ford algorithm
for each edge(u, v) ∈ G'.E
ŵ(u, v) = w(u, v) + h(u) − h(v)
let D = (d_uv) be a new n × n matrix
for each vertex u ∈ G.V
run Dijkstra(G, ŵ, u) to compte δ2(s, v)for all v ∈ G.V
for each vertex v ∈ G.V
d_uv = δ2(u, v) + h(v) - h(u)
return D

## 第 150 页

## 第 151 页

## 第 152 页

2. 基本的 Ford-Fulker 方法
在 Ford-Fulkerson 方法的每次迭代中，寻找某条增广路径 p ，使得 p 来对流 f 进行修改 ( 增加 ) 。简单
来说就是两步：
基本的 Ford_Fulkerson 算法
基本的 FF 算法仅仅是对 ford_fulkerson_method 过程的简单扩展。
Ford_fulkerson_method(G, s, t)
initialize flow f to 0
while there exists an augmenting path p in the residual network Gf
augment flow f along p
return f

## 第 153 页

FF 算法的运行时间是取决于第三行是如何寻找增广路径的。使用 BFS 来寻找增广路径，算法的运
行时间是多项式的。
这里我们用f ∗表示网络中一个最大流，则在 FF 算法的一个直接实现中，执行第 3~8 行的 while 循环
的次数最多为|f ∗|次，因为流量值在每次迭代中最少增加一个单位。如果使用深度优先搜索或广
度优先搜索，在一个残存网络中找到一条路径的时间应是O(V+E) =O(E)。 while 循环每一遍执
行所需的时间为 O(E) ，所以 FF 算法的运行时间为O(E|f ∗|)。
Ford_Fulkerson(G, s, t)
for each edge(u, v) ∈ G.E
(u, v).f = 0
while there exists a path p from s to t in the residual network Gf
cf(p) = min{cf(u, v):(u, v) is in p}
for each edge(u, v) in p
if(u, v) ∈ E
(u, v).f = (u, v).f + cf(p)
else (v, u).f = (v, u).f - cf(p)

## 第 154 页

Edmonds-Karp 算法
算法中寻找增广路径的操作中使用广度优先搜索来改善 FF 算法的效率，这种方法是 EK 算法。 EK
算法的时间复杂度是O(VE2)。

## 第 155 页

将单轮 BFS 增广的复杂度与增广轮数的上界相乘，我们得到 Edmonds_Karp 算法的时间复杂度是
O(|V||E|2)。
(https://www.acwing.com/problem/content/2173/)
//C++ 版本 EK 算法
[2171. EK 求最大流 - AcWing 题库 ]

## 第 156 页

27. 二分图中的匹配 (Matchings in Bipartite Graphs)
28. 字符串匹配算法
29. 数论
30. 计算几何
1. 凸包
31. 多线程算法
